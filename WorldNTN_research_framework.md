# WorldNTN：面向 ICC 六页论文与 Poster 的研究框架

> **更新：2026-09-11。** 本轮已通读原稿第 0–63 节，并根据“已有 NTN 建模、仿真、波束/抗干扰资源、GPU，以及一位擅长大模型的合作者”重新评估选题。下面的 **R0–R12 是当前建议**；后面的历史原稿完整保留，便于追溯。
>
> **当前原则：论文含金量优先。** 按用户最新要求，11 月 1 日保留为期望交付节点，预计延期不用于压缩研究问题；不按固定日期砍掉重要机制或提前锁定方案。六页限制最终表达，不限制前期研究深度。
>
> **阅读顺序：** 先看 R0 的贡献标准、R3 的方向比较、R6 的核心机制、R8 的证据要求和 R10 的研究里程碑，再读 R4 的物理层实例与其他技术细节。
>
> **证据状态：** 文献事实附来源；新方法、创新判断、参数与预期收益均为待验证的研究建议。本文没有新实验结果，也不把“尚未检索到同题论文”当作首创证明。

## R0. 先给结论：用科学问题和贡献深度选择主线

原稿最有价值的思想是：**已知物理直接计算，学习剩余未知动态，再比较动作的未来后果。** 应继续保留。现在不以“能最快完成哪个模型”为选题标准，而是追问：它揭示或解决了哪个现有方法尚未充分处理的通信问题？

“Graph + RSSM/JEPA + MPC + anomaly + recovery”可以是一条研究路线，但模块齐全不等于贡献成立。同样，“二阶统计预测 + 鲁棒波束控制”虽然适合现有资产，也不能仅凭组合就认定是含金量最高的主线。

### 当前最值得深入检验的主线

> **在部分可观测的 NTN 中，利用已知未来几何和学到的环境动态，推演“测量什么、采取什么控制”对未来信息与通信性能的共同影响。**

把原来的 A（波束控制）和 C（主动测量）放在同一个科学问题下考虑。波束/零陷是一个适合检验该问题的物理层场景；关联/切换也可以成为载体。具体载体由问题的重要性、可辨识性、紧邻工作差异和实验证据决定，不因已写了某套公式就提前锁定。

这里可能形成深度的三个位置是：

1. **任务相关的环境表征。** 研究哪些隐状态或统计量足以区分候选动作的后果，哪些细节可以舍弃，以及这种表征在换几何、换动作集合或换控制目标后是否仍有效。二阶统计是可验证的候选，不预设其对所有任务充分。
2. **信息获取与控制的耦合。** 某个量最不确定，不代表最值得测；测量的价值取决于它是否会改变未来可执行的通信决策。模型需推演测量返回后的 belief 更新和后续控制，而不只外推 KPI。
3. **未见扰动下的干预与适应。** 若选择抗干扰主线，应研究如何区分对控制而言不同的扰动机制、用什么观测/动作消除歧义，以及模型如何据此更新。检测正常性偏离只解决其中一小部分。

**当前优先深入第 1–2 项的关联机制，同时保留第 3 项作为独立竞争方向。** 这不是断言主动测量或 dual control 本身新颖；它们有成熟研究基础。需要证明的应是 NTN 特有的信息可获得性、未来几何和物理约束如何改变问题，以及所提方法相对相应强方法的实质区别。

### 什么样的结果才值得写成这篇论文

- **问题重要：** 真实的反馈/观测限制、服务连续性或干扰约束，使当前方法存在明确缺口。
- **机制清楚：** 能指出收益来自哪个建模或决策机制，并通过受控实验把它分离出来。
- **必要性成立：** 比较预测加优化、经典状态估计、信息感知控制等合适对手，说明学习模型为何有用。
- **结论可迁移：** 不只在一个人为设置中提升几个百分点，还解释收益区间、失败条件和跨场景表现。
- **表达聚焦：** 六页围绕一个中心命题组织；支撑这个命题的模型、理论和验证可以充分展开研究。

11 月 1 日仍是期望交付节点，按用户对延期的预期安排研究；投稿日程作为行政事项单独核对，不再用此前公布的日期倒推或降低研究目标。Poster 与论文共享同一个经证据支持的结论。

## R1. 原稿中必须修正或补上条件的论点

| 原稿位置/论点 | 修订判断 | 对第一篇论文的影响 |
|---|---|---|
| 第 1–2 节：DT 描述现在，WM 描述未来 | 这是项目内的分工，不是通用定义；DT 也能预测、优化并接收动作输入 | 比较具体能力与信息条件，避免贬低 DT 来证明 WM 必要性 |
| 第 4、21、24 节：把 queue、load、channel 都交给网络学习 | 队列守恒、已知调度规则、链路预算和波束响应应尽量显式计算；未知到达、遮挡、活动状态或模型误差才需要学习 | 从“所有量的预测器”转为结构化混合模型 |
| 第 6、9 节：未来物理是真值 | 可用的是决策时已有星历/位置估计推算出的未来；轨道、姿态、UE 位置和同步仍有误差 | 所有方法获得相同的未来物理信息，并做误差敏感性测试 |
| 第 7、36 节：RSSM 天然具有 uncertainty | 随机 latent 不等于可靠的认知不确定性，更不等于校准好的风险概率 | 分开评估过程随机性、模型分歧与实际覆盖率 |
| 第 7、37–38 节：JEPA 特别适合异常检测 | 预测表征可能忽略控制无关噪声，也可能把异常压掉；仅 stop-gradient 不足以说明训练不会塌缩 | 不以“更时髦”为模型选择理由；需要目标编码器更新、防塌缩设计和任务验证 |
| 第 9.5 节：物理一致性 loss | 若输出本来定义为 physics + residual，再惩罚同一个恒等式，该项恒为零 | 用硬结构约束，或与独立物理关系/测量构造有效损失 |
| 第 12、39–40 节：WM 比 DRL 更有研究价值、统一框架就是 novelty | 价值要由具体难题和证据决定；普通 model-based control 已能复用模型、换代价函数 | “换网络”与“加模块”不算核心贡献 |
| 第 15–19、43、57–58 节：正常模型检测未知攻击后规划恢复 | 低正常似然只说明偏离正常分布；正常模型可能在攻击后系统性预测错误 | 检测能力不能代替攻击态动力学与恢复能力的证据 |
| 第 25 节：干扰功率公式 | 链路预算在 dB/dBm 域相加减；SINR 分母里的功率必须统一转换到线性域 | 防止仿真单位错误制造虚假收益 |
| 第 43、59 节：地面 jammer 出现后 handover 即可恢复 | 同一终端、同频、近似全向接收时，换卫星可能仍受到同一 jammer；改善需要方向图、频率或空间差异 | 先验证动作有物理控制能力，再设计恢复算法 |
| 第 48 节：加入随机动作即可得到 counterfactual | 动作覆盖是必要条件；日志中的隐藏混杂、动作后信息和缺失观测仍可能导致伪因果 | 仿真中做受控分支实验；实测日志不能自动解释为因果效应 |
| 第 49 节：加 uncertainty penalty 即可 safe | ensemble 可能共同犯错，经验惩罚不保证真实约束满足 | 初稿用“风险感知/鲁棒”，有明确假设与证据才用“安全保证” |
| 第 59–60 节：每个 UE 有 3–8 个可见候选星 | 这是期望实验规模，不是所有轨道、位置、仰角门限下的事实 | 候选数由物理可见性、波束覆盖与接入能力生成，并报告其分布 |

还有一条比选网络架构更关键：

> **World model 不会使本来不可预测的随机量变得可预测。** 如果未来干扰独立同分布、当前信息已充分，复杂时序模型不应稳定优于条件统计量加优化。这个负对照必须保留。

## R2. 更新后的文献位置：已有工作覆盖了哪些部分

本轮核对了原稿列出的 7 篇 arXiv 论文的题名/摘要，并查看了部分紧邻工作的相关方法段落。以下是选题定位表，不等于已完成系统综述或完整复现。除明确标注的会议信息外，不把 arXiv 预印本当作已经发表或录用的论文。

| 相关工作 | 已覆盖的能力 | 本项目需要进一步证明的区别 |
|---|---|---|
| [Telecom World Models，2026](https://arxiv.org/abs/2604.06882) | 动作条件化、不确定性、预测规划的电信 WM 架构 | 不能贡献“提出 telecom WM”；必须落到 NTN 的具体结构与通信结果 |
| [A Wireless World Model，2026](https://arxiv.org/abs/2603.25216) | 基于 CSI、几何和轨迹的预测表征 | 不能贡献“预测无线信道”；需证明所学统计对候选控制动作与低反馈决策有效 |
| [Physics-Informed NTN Digital Twins，2026](https://arxiv.org/abs/2605.23155) | 物理先验、稀疏/过时导频下 CSI 重建、轨道相关流量预测 | physics + residual 本身已有直接相邻工作；应比较闭环决策而非只比较预测误差 |
| [PreHO，2026](https://arxiv.org/html/2603.07987v1) | 利用 LEO 可预测性规划切换，包含资源分配与关联优化 | 不能把对手简化成只看仰角；研究有限观测、随机残差和业务代价是否带来额外问题 |
| [NEO-GNN，2026](https://arxiv.org/abs/2606.31950) | 异构图表示卫星、网关、地面小区并优化关联 | 图编码器是工具；规模泛化若为贡献，就要真实测试节点数量与拓扑变化 |
| [DWM-RO，2025；v3 更新于 2026-06](https://arxiv.org/html/2511.05972v3) | 星地网络中以 WM 支持波束/功率相关控制、想象训练与不确定性触发协调 | 不能声称首次 WM + 卫星或 WM + 波束；重点比较物理结构、反馈条件与多步动作评估 |
| [卫星–UAV 预测抗干扰，2026](https://pubs2.ascee.org/index.php/IJRCS/article/view/3090/0) | 出版方可检索摘要已涉及延迟观测、ensemble WM、风险约束及多类抗干扰动作 | 与原稿“大统一抗干扰闭环”直接重叠；全文访问受限，须补读后才能作更细的创新判断 |
| [Robust Beamforming Avoiding Satellite Interference，VTC 2025-Fall](https://orbilu.uni.lu/handle/10993/66546) | 作者机构库摘要确认 sCSI、干扰/功率约束下的星地共存波束设计 | “二阶统计 + 鲁棒波束”也不是新概念；需要突出动态预测与测量成本的实际增益 |
| [JEPA for AI-Native 6G，2026](https://arxiv.org/abs/2607.09798) | JEPA 通信教程及波束管理示例 | JEPA 不是天然创新点，更不是一定优于 GRU/RSSM 的结论 |
| [World Models Under Asynchronous Sensor Observations，2026-09-07](https://arxiv.org/abs/2609.07299) | 已研究陈旧度、刷新时间与异步观测下的 WM | 仅把 observation age 拼到输入不够；需要通信特有的动作/信息/约束关系 |

额外需要注意的邻域：[WONDER](https://arxiv.org/abs/2608.16955) 已研究候选轨迹的无线影响与多 UAV 协调；[SCD integration](https://arxiv.org/abs/2606.30568) 已涉及预测不确定性与感知更新。因此，“动作后果预测”和“按需测量”也要具体到本文问题，不能泛称首创。

原稿引用的 [ETSI TR 104 319 工作项](https://portal.etsi.org/webapp/WorkProgram/Report_WorkItem.asp?WKI_ID=79357) 在本次页面上仍为 early draft；适合作为背景，不是成熟规范符合性的依据。原稿 [3GPP Release 20 页面](https://www.3gpp.org/specifications-technologies/releases/release-20) 本次访问受限，其细节不作为当前创新判断依据。

**建议的 gap 表述：**

> 在可预知的 LEO 几何之上，能否仅利用有限、陈旧的反馈，学习足以评估未来控制动作的无线环境统计，并在相同测量、计算和物理约束下改善通信性能？

这仍是**待通过更细文献对照与实验确立的研究空白**。它比“统一预测、检测和恢复”更容易形成可审查的技术证据。


## R3. 候选方向比较：围绕物理问题选择 world model

下表按问题结构比较研究潜力，不按完成速度排序。A/B 是通信任务，C/E 更接近可能的核心机制，不能把它们视为互斥项目。已有资产用于提高验证质量，不用于替代创新判断。

| 方向 | 真正需要学习的未知量 | 动作与未来的联系 | 最有说服力的结果 | 贡献机会与关键质疑 |
|---|---|---|---|---|
| **A. 预测式干扰约束波束控制** | 未来信道二阶统计、空间干扰活动、遮挡/姿态残差 | 波束、零陷、功率决定期望信号/泄漏及连续服务；测量决定未来信息 | 同等干扰违约率和反馈量下提高有效速率；跨场景/反馈延迟仍有效 | 适合检验任务相关表征与信息控制；必须超越统计预测 + 鲁棒优化 |
| **B. 有限观测下的预测关联/切换** | 候选链路残差、背景负载、业务突发和隐状态 | 切换改变资源竞争、队列服务和中断；候选链路测量影响下一次选择 | 同等切换次数/测量开销下减少 outage 与业务丢失 | 适合检验观测选择与资源/业务耦合；不能只重复轨道预测切换 |
| **C. 主动测量与控制联合规划** | 未观测链路或干扰场的 belief | 先测某位置/波束，后续控制因信息而改变 | 同一 KPI 下减少反馈/导频；同一预算下减少违约 | **优先深入的机制候选**；必须比较经典 dual control/信息感知规划，不能只加不确定性阈值 |
| **D. 多星/多波束预测资源协调** | 动作相关的干扰、拥塞与其他控制器反应 | 多主体动作耦合，短视局部最优可能损害整体 | 跨规模、多负载下提高吞吐/公平性，并控制规划时延 | 若研究对象是多主体外部性与可迁移动态，复杂性有理由；仅扩大网络或加 GNN 不够 |
| **E. 异常下的短时适应与鲁棒恢复** | 扰动状态及其演化、模型失配 | 控制改变暴露或抑制干扰；攻击者可能响应 | unseen 扰动下的恢复代价/速度，超过变点检测 + 规则/鲁棒控制 | **独立竞争主线**；核心应是扰动可辨识性、干预与适应，不能只用正常模型直接恢复 |
| **F. 网关/馈电链路天气多样性** | 空间相关衰落、业务和网关可用性 | 切网关、路由、缓存改变未来交付 | 预测天气残差时的 outage–切换/带宽 tradeoff | 需解释相关天气、观测和切网关的决策耦合；数据工程量不决定学术价值 |
| **G. 接触窗口内的缓存/预取/排队** | 需求与剩余服务能力 | 当前预取占用容量，但减轻未来断连损失 | 交付率、截止期违约和数据新鲜度 | 很适合结构化 WM；须超过 contact-plan optimization + traffic predictor |
| **H. 少量反馈校准高保真 RF 仿真代理** | 几何/材质/天线失配、真实残差 | 候选控制查询代理，少量新测量用于校准 | sim-to-measurement 转移、决策误差与计算收益 | 可深挖失配校准与有效迁移；单纯替代昂贵仿真不自动构成 WM 控制贡献 |

### R3.1 进一步区分“可发表的应用”与“有更深贡献的问题”

| 可能的中心命题 | 较弱的版本 | 值得争取的版本 | 决定性验证 |
|---|---|---|---|
| 学习控制所需的世界状态 | 预测 Q，再接既有优化器 | 表征对动作后果有效、对无关变化稳定，并能在新布局/任务下复用 | 相同数据、参数量与 planner 下的动作排序、控制收益和迁移 |
| 测量与控制联合规划 | 不确定性高就测 | 测量选择由后续可执行决策的价值决定，利用已知未来几何安排反馈 | 同预算下优于最大不确定性、约束裕量规则和经典 belief-space 方法 |
| 异常下适应 | 检测到误差大就切换 | 区分需采取不同措施的隐藏机制，选择有辨识力的观测/合法动作并更新模型 | 初始观测相似但正确响应不同的配对场景，以及未见扰动机制 |
| 模型复用 | 换 reward 后还能运行 | 冻结环境模型后改变保护门限、成本或任务，少量或无需新动态训练仍能正确决策 | 严格控制预训练数据与适应预算，并与模块化预测控制比较 |

较小的 KPI 提升不必然意味着贡献弱；揭示必要条件、证明某种状态表征足够或给出可信的失效边界，同样可能有价值。反过来，一个人为场景中很大的提升也不自动代表有深度。

**原稿未充分利用的三个 NTN 特点：**

- **未来几何已知，但观测机会并不无限。** 某条链路即将可见，不代表已经有它的实时 CSI 或可用容量；已知可见窗口可以帮助决定测量是否值得。
- **控制完成需要时间，且可行动作会过期。** 波束调整、反馈返回、切换执行均应按实际时序计入，不能让网络在未来时刻执行已失效的动作。
- **对控制有用的状态可能远小于完整物理状态。** 非相干干扰可能只需要二阶统计；业务控制可能只需要队列、服务分布和可见窗口。研究“需要预测什么”常比加深模型更有价值。

这些是问题结构和设计机会，并不单独保证 novelty。

## R4. 物理层候选 A：面向低反馈频谱共存的二阶统计世界模型

### R4.1 问题定义与论文题目

中文工作题目：

> **稀疏反馈下基于二阶统计世界模型的 LEO 预测式干扰约束波束控制**

英文暂定题目：

> **WorldNTN: Predictive Beam Control with Sparse-Feedback World Models for LEO–Terrestrial Coexistence**

假设一个 LEO 下行系统与地面网络共享研究频段。卫星为 NTN 用户服务，同时限制对受保护地面接收机的干扰。此处“干扰门限”是明确给定的系统设计约束；未经对应频段/场景核查，不宣称满足某项法规。

第一版保持：

- 一个调度区域；一颗受控卫星或一组已固定关联的服务波束；
- 固定用户关联与频谱分配，只优化波束/零陷候选及必要的功率档位；
- 地面受保护接收机或测量点数量有限；跨网络反馈可获得性必须由系统接口解释；
- 星历、用户/保护点位置、天线阵列模型由你的 NTN 仿真提供；
- 反馈稀疏、延迟和有噪声，控制器看不到每个接收机的实时完整信道；
- 背景遮挡、地面活动或姿态误差只选择一至两种有物理依据的未知动态。

这一节给出可推导、可检验的物理层载体，不把它预先认定为最终贡献。固定测量下的控制既可用于诊断模型价值，也应作为 R6 联合信息控制的重要基线。

若现有资产更适合“地面接收阵列抑制上行干扰”，可以交换收发角色，但应另行重写信号模型。不要在同一套公式中混用发射零陷、接收 MVDR 和地面 jammer 检测。

### R4.2 为什么二阶统计值得研究

设卫星给 NTN 用户 u 的发射向量为 w_u，包含发射幅度，满足总功率约束。到受保护接收机 v 的信道为 g_v。若各用户发送的数据符号彼此不相关，平均泄漏功率为：

$$
I_v(t)=\sum_u \mathbf w_u(t)^H\mathbf Q_v(t)\mathbf w_u(t),
\qquad
\mathbf Q_v(t)=\mathbb E[\mathbf g_v(t)\mathbf g_v(t)^H\mid \xi_t].
$$

这里 ξ_t 表示当前环境条件。期望取在明确的短时统计窗口/快变随机量上；控制器并不直接知道 ξ_t 或 Q_v。不同卫星间也只有在相应独立性/非相干假设成立时，才能直接相加功率。

于是研究对象可以是 **未来 Q_v 的条件分布**，而非所有复数信道的每个随机相位。对给定环境条件，上式保留了不同候选波束之间的可计算关系。

必须限定：

1. Q_v 保留空间相关性，并非简单把所有阵元功率相加。
2. 若存在相干联合发送或相关数据流，需要联合协方差与交叉项，上式不能直接套用。
3. 随机化每条链路的公共整体相位不改变 ggᴴ；随机化阵元间相位会改变它。验证非相干鲁棒性时不能把两者混淆。
4. Q_v 对平均二次型泄漏足够，但通常不足以恢复瞬时干扰尾概率，也不足以精确计算平均 log(1+SINR)。若只预测二阶统计，就应把约束定义为对应窗口的平均泄漏；若要保证瞬时 outage，需要进一步的分布假设或样本模型。

**通信层面的潜在贡献：找到足以支持目标控制任务、同时不必追踪无关相位的预测状态。** “充分”仅针对上述假设和统计量，不泛称对整个 NTN 充分。

### R4.3 物理分解与可学习部分

物理层直接提供距离/角度/名义增益和阵列响应。模型学习剩余环境状态，通过结构化解码器输出有效协方差，例如：

$$
\widehat{\mathbf Q}_{v,t+k}
=
\mathbf L_{v,t+k}\mathbf L_{v,t+k}^{H}
+\epsilon_Q\mathbf I,\qquad \epsilon_Q>0.
$$

L 可使用低秩因子，并以已知几何响应为输入。ε_Q 的单位与协方差一致，不能用任意常数掩盖低功率场景。相对物理基准的 residual 可以存在，但最终 Q 必须保持半正定；直接给每个复数矩阵元素加无约束 residual 可能产生负功率。

推荐区分三类量：

| 类别 | 例子 | 处理方法 |
|---|---|---|
| 已知外生上下文 c_t | 星历推算的几何、名义传播、合法动作掩码 | 解析计算，所有基线共享 |
| 隐环境 ξ_t | 慢变遮挡、干扰活动、统计信道残差 | 从观测历史推断并预测分布 |
| 可控运行状态 x_t | 当前波束/功率、切换/重配置计时器、业务队列 | 能解析更新的直接更新，其余才学习 |

**不要强行让动作改变自然信道。** 若本场景中环境与本方波束无关，可以令：

$$
p_\theta(\xi_{t+1}\mid \xi_{\le t},c_{\le t+1})
$$

不含控制动作；再用已知链路方程构成动作条件化的完整转移与 KPI 预测。动作确实会改变队列、服务、测量质量和资源占用。只有存在自适应干扰方、波束间调度耦合等机制时，才学习对应的动作相关环境转移。

这意味着，“信道统计预测 + 解析波束响应 + 控制状态更新”本身也可视为混合 world model。论文要证明改进在哪里，不能靠重新命名排除这种强基线。

### R4.4 观测、状态和在线可用信息

定义：

$$
o_t=(y_t,m_t,\Delta_t),\qquad
b_t=q_\phi(\xi_t\mid o_{\le t},a_{<t},c_{\le t}),
$$

其中 y 是实际收到的导频/反馈/功率测量，m 是缺失掩码，Δ 是每个量的观测年龄；b 是模型内部对隐藏状态的 belief，可由 GRU 或 RSSM 表达。

每条数据应有“测量发生时间”和“控制器收到时间”。若报告在 t 测量、t+d 才返回，那么 t 到 t+d 之间的决策不许使用它。历史动作只能截至 a_{t-1}；候选 a_t 进入预测器，不能被伪装成已观测结果。

跨网络反馈不是免费的：

- 能否获得保护点位置、活动标记、功率报告或 CSI 摘要，须分别说明。
- 只有标量干扰测量时，未必能识别任意维度的 Q；需要低秩/角域先验和足够多样的已知探测方向。
- 可以从“有延迟的低维统计反馈”建立参照，再研究仅标量反馈的识别条件；若低反馈可辨识性本身成为中心贡献，应正面分析它，而不是因实现难度提前排除。
- 仿真器隐藏真值可用于离线监督和评估，但所有在线方法只能接收声明过的观测。若模型训练依赖仿真真值，应额外报告仅使用可获得反馈训练/校准的版本或明确适用边界。

### R4.5 控制目标与可执行规划

令 a_t 表示有限集合中的波束/零陷方案及功率档位，W_t 为其对应向量集合。先用现有物理算法生成候选，如常规波束、不同零陷宽度、鲁棒加载程度和功率退避；从 8–32 个候选配置起步，数量由运行时实测调整。复杂连续波束搜索留作升级。

规划 H 步：

$$
\max_{a_{t:t+H-1}\in\mathcal A_{\mathrm{phys}}}
\mathbb E\!\left[
\sum_{k=0}^{H-1}\gamma^k
\bigl(R_{t+k}^{\mathrm{eff}}-\lambda_{\mathrm{sw}}C_{t+k}^{\mathrm{sw}}\bigr)
\right],
$$

$$
\text{s.t.}\quad
\sum_u\|\mathbf w_{u,t+k}\|_2^2\le P_{\max},\qquad
\widehat{\Pr}\!\left(
\max_{v,k}\frac{I_v(t+k)}{I_v^{\max}}>1
\,\middle|\,b_t,a_{t:t+H-1}
\right)\le\delta.
$$

- R_eff 使用已定义的有效服务速率/交付量，扣除重配置与测量占用；“Shannon 速率”“平均速率近似”和真实 goodput 不混称。
- I_v 按 R4.2 的平均窗口定义。概率来自预测的慢变环境不确定性；不能据此声称约束了未建模的瞬时尖峰。
- 上式是所有保护点、所有预测步的**联合风险**。若实现的是逐点逐步约束，应分配风险预算或明确它只控制边际风险。
- 帽号表示模型估计，真实违约率必须在独立测试中计算。
- 只执行第一个控制动作；新反馈到达后更新 belief 并重新规划。
- 重配置状态、反馈传输和控制完成时间都进入状态。若某动作生效前服务窗口已结束，直接判无效。

机制验证可从 fixed-H、有限动作候选和小规模 beam search 开始。是否扩展连续动作、反馈分支或自适应 horizon，由中心命题决定。候选集合、剪枝预算和硬件对所有 planner 保持一致。不要把“只有本方法使用更大的搜索预算”误当成模型提升。

### R4.6 一个可以解释物理结构价值的小推导

若真实 Q_v 与预测 Q̂_v 都为 Hermitian，并且某时刻有：

$$
\|\mathbf Q_v-\widehat{\mathbf Q}_v\|_2\le\varepsilon_v,
$$

则：

$$
|I_v-\widehat I_v|
\le
\varepsilon_v\sum_u\|\mathbf w_u\|_2^2
\le\varepsilon_v P_{\max}.
$$

因此在该误差条件成立时，使用：

$$
\widehat I_v+\varepsilon_v P_{\max}\le I_v^{\max}
$$

可得到该时刻平均泄漏的保守约束。推导来自标准二次型不等式，不作为独立原创定理。

它的价值是建立“统计预测误差—可用发射功率—干扰裕量”的直接联系。**关键难点在 ε_v 的可信获取**：训练/验证集上的分位数不自动构成 OOD 条件下的确定性上界；多点、多步还需处理联合事件。若只经验校准，应称经验风险控制，不能包装为无条件安全证明。


## R5. 模型和训练：先验证结构价值，再决定模型规模

### R5.1 最小架构

~~~text
星历/位置/阵列模型 ───────────────→ 未来物理上下文、动作有效性
                                             │
稀疏反馈 + 时间戳 + 历史动作 → 递归状态估计器 → 隐环境分布
                                             │
候选波束/零陷/功率 ──────────→ 解析信号层 + 控制状态转移
                                             │
                                  多步服务/干扰/成本分布
                                             │
                                      风险感知规划
                                             │
                            执行首个动作 → 实际反馈
~~~

按以下顺序升级即可：

| 版本 | 实现 | 要回答的问题 |
|---|---|---|
| M0 | 物理模型 + persistence/AR/Kalman 或 HMM/IMM + 解析控制 | 复杂学习是否必要？ |
| M1 | 小型 GRU 状态估计器 + 结构化协方差/残差解码器 | 非线性历史是否带来价值？ |
| M2 | 3–5 个 M1 的 bootstrap ensemble；必要时采用 RSSM | 多步预测分布和模型分歧能否改善实际约束表现？ |
| M3 | 参数共享的角域/集合编码器，或轻量 Transformer | 跨用户、保护点、布局与轨迹的泛化是否改善？ |
| M4 | JEPA/更大预训练模型、teacher–student 蒸馏 | 在相同数据预算和真实推理预算下是否值得增加复杂度？ |

M2 的个数只是起始计算预算。采用 RSSM 时要包含 prior/posterior、KL 正则和稳定训练细节，不是只写一个 latent 高斯分布就完成了概率模型。PlaNet 为随机递归状态与规划提供方法参考；PETS 为 ensemble 与轨迹采样提供基线思路。[PlaNet](https://arxiv.org/abs/1811.04551)、[PETS](https://arxiv.org/abs/1805.12114)

Dreamer 在想象中训练策略，TD-MPC2 研究面向控制的 latent 模型；它们可作为方法背景，但不必全部复现。当前有限离散动作适合直接规划，若未来变成连续高维动作，再评估相应算法。[DreamerV3](https://arxiv.org/abs/2301.04104)、[TD-MPC2](https://arxiv.org/abs/2310.16828)

### R5.2 训练目标应该贴近“选对动作”

单纯降低全状态 MSE 可能不改善控制。第一版优先包含：

$$
\mathcal L
=
\lambda_{\mathrm{obs}}\mathcal L_{\mathrm{observed}}
+\lambda_{\mathrm{roll}}\mathcal L_{\mathrm{multi-step}}
+\lambda_{\mathrm{dec}}\mathcal L_{\mathrm{decision}}
+\lambda_{\mathrm{KL}}\mathcal L_{\mathrm{KL}},
$$

其中最后一项仅在需要的概率 latent 模型中使用。

- **Observed loss：** 对实际可观测量或声明使用的仿真监督量做 NLL/适当的回归损失，缺失值按 mask 处理。
- **Multi-step loss：** 从同一历史出发，按真实记录的动作序列自由展开；测试时不能每一步输入真实下一状态。
- **Decision loss：** 关注候选波束的泄漏功率、效用或排序，而非矩阵每个元素都等权。可使用 beam-projected 误差：
  
$$
\mathcal L_{\mathrm{beam}}
=
\sum_{v,k,a\in\mathcal A_{\mathrm{train}}}
\left(
\sum_u\mathbf w_u(a)^H
(\widehat{\mathbf Q}_{v,t+k}-\mathbf Q_{v,t+k})
\mathbf w_u(a)
\right)^2.
$$

这里的 Q 真值若来自仿真器，属于额外训练监督，必须给相应基线相同数据条件。只对有限训练波束投影拟合会留下不可识别方向，因此还需测试未参与该损失的波束/零陷配置。

也可加入分支轨迹的 return ranking，但应先证明它带来收益再纳入最终方法。无需为每个可能模块写一个 loss。

### R5.3 动作覆盖与反事实验证

训练数据用多种合法策略采集，覆盖保守波束、不同零陷宽度、功率档位以及各种测量模式。分清：

- **未来预测：** 当前策略继续运行会怎样？
- **干预后果评估：** 从相同状态执行其他合法动作会怎样？
- **未支持动作外推：** 日志从没见过这种动作，模型是否有理由预测？不能默认有。

仿真器应支持保存/恢复状态，然后对候选动作分支运行。共享的是外生随机过程，如预先生成的天气/到达/背景干扰轨迹；动作造成的队列、服务和观测差异仍按各分支演化。

**仅把随机种子设为相同还不充分。** 不同动作可能消耗不同数量的随机数，最好按“episode、时间、实体、扰动类型”索引外生过程。分支真值只用于训练/离线验证，最终控制测试中 planner 不得调用隐藏 simulator oracle。

### R5.4 不确定性评估必须落实到控制量

至少区分：

- 环境本身的随机性：即使模型正确也无法消除；
- 模型不确定性：数据不足或分布变化造成的分歧；
- 测量噪声、延迟与缺失造成的状态不确定性。

需要报告预测区间覆盖率与宽度，且按 horizon、反馈年龄和场景分别统计。只给一个全局 Gaussian NLL 或 ECE 不足以说明每个保护点的风险估计可靠。规划使用的 tail risk，要和独立测试中的真实违约频率对应起来。

时间相关数据不能简单套用 i.i.d. 校准保证。采用校准分位数或 conformal 思路时，应明确其假设、episode/block 划分及 OOD 下的限制。检测分数也不必把 Mahalanobis、Gaussian NLL 与 latent residual 无解释地相加，因为它们可能重复计算同一信息。

## R6. 优先深入的机制：任务相关的信息获取与控制

原稿主要讨论控制动作，忽略了**获取信息本身也是通信决策**。

例如，预算只允许更新一个保护点或一个候选波束的统计。测最不确定的点未必最佳：该点可能不影响任何可行波束；一个不确定性较小但接近干扰门限的点，可能更值得测。

令 u_t 为测量动作、a_t 为控制动作，测量结果 y 在延迟 d 后返回。信息价值可写成：

$$
\operatorname{VOI}(u_t)
=
\mathbb E_{y\sim p_\theta(y\mid b_t,u_t)}
\left[V(b_{t+d}^{\,u_t,y})\right]
-
V(b_{t+d}^{\,\mathrm{no\ new\ report}})
-
\lambda_m C(u_t).
$$

两个分支都要推进到同一个 t+d；等待期间的控制、服务损失和其他观测按相同规则处理。b 的更新使用模型预测的观测分布，不能提前看到未来真实测量。只有预算允许时才执行所选测量，结果到达后再更新控制。

可以先用下列受控实验定位机制，再扩展为“任务相关的测量选择 + 混合 world model”。这些简化用于建立清楚的比较，不是最终研究深度的上限：

- 同一反馈预算下，选一个待测对象；
- 固定周期、随机选择、最大不确定性选择、接近约束门限选择作为基线；
- 对比同等测量次数下的真实通信收益，而非只比较 belief entropy；
- 只做一步信息前瞻，控制部分仍用短 MPC。

**成立条件：** 在线反馈路径可解释、观测结果能改变相关 belief、改变后的信息确实可能影响后续动作，并且收益扣除了测量与等待成本。固定测量下单独提升不大，并不自动否定联合信息控制；其贡献可能恰好来自两者的耦合，需用分解实验识别。

另一个后续方向是“何时相信模型、何时退回物理鲁棒策略”。可以用独立验证中建立的误差—风险关系触发，但阈值法不是安全保证；也不能把 fallback 的所有收益都归因于 world model。

### R6.1 这条主线怎样才能有实质贡献

**第一步：证明“预测准确”与“信息有价值”是不同的问题。**

可以构造两组控制器当前估计误差相近的场景：

- 场景一：某保护点的干扰统计很不确定，但无论测量结果如何，所有合法波束都远离门限，或者剩余服务时间不足以利用新报告。
- 场景二：另一个保护点的不确定性更小，但报告可能使最优决策在“窄零陷”和“功率退避”之间改变，且报告能在动作仍有效时返回。

这个配对例子是建议构造的机制实验，不是已有结果。它检验“最大不确定性”是否等价于“最大通信价值”。还应设置条件改变后结论反转的情况，避免只构造有利例子。若几条简单的约束裕量/剩余窗口规则已能解决，应进一步明确 WM 负责的未知动态与长时影响在哪里。

**第二步：模型必须展开不同测量结果对应的后续控制。**

仅预测一条平均未来、再执行固定动作序列，可能忽略测量价值。较完整的规划需要：

~~~text
候选测量/控制
      ↓
可能的反馈结果及其概率
      ↓
各分支的 belief 更新
      ↓
各分支重新选择后续合法控制
      ↓
扣除反馈、等待、重配置成本后的期望通信收益
~~~

可用低分支数 belief tree、采样近似或学习到的 belief value 来实现。计算规模应是被测量、被解释的代价，不以“太难做”为理由直接忽略决定机制是否成立的分支。

**第三步：把已知未来几何嵌入信息价值，而不只拼成特征。**

未来可见性、空间角度和剩余服务窗口影响哪些测量会返回、何时可用、哪些动作届时仍可执行。需要比较“相同物理信息仅作为输入”和“显式约束观测/动作分支”的区别。若只是多一个时间特征，贡献应如实定位，不能称 NTN 专用新理论。

**第四步：与足够强的估计和信息规划方法对比。**

至少包含固定测量、最大不确定性、基于干扰裕量/服务窗口的任务规则，以及采用经典状态估计模型的 belief-space/信息感知规划。必要时加入 generic learned-belief planner，分离“用了学习模型”和“NTN 结构有用”。

采用一个 2×2 实验：环境估计/预测为“最强简单模型或所提模型”，测量决策为“最强任务规则或带观测分支的规划”；其余控制器、预算、数据一致。若收益主要来自通用观测分支规划，就不能全部归因于新的世界模型。

在可解的小问题中，加入已知生成模型的参考控制器，帮助区分模型误差和规划近似误差；明确其额外知识，不自动称严格上界。再用机制更复杂、解析模型不充分的环境检验学习是否必要。

**第五步：理论分析服务于具体结论。**

可以选择研究一个问题，而不要求全部完成：

- 哪些反馈与探测方向足以区分“需要不同最优控制”的隐藏环境？
- 统计模型误差如何传递到动作价值或约束余量，能否得到有实际意义的误差预算？
- 在哪些条件下未来几何使某项测量的控制价值为零，或使其相对优先级改变？
- 如果舍弃某类相位/环境细节，在哪些动作与目标集合上仍保持正确的决策关系？

上述都是待研究命题。标准二次型不等式、常规误差上界或直接套用 POMDP 表述不能独立充当原创贡献。反例和负结果若清楚限定了 WM 的必要性，也应保留。


## R7. 网络层候选 B：面向业务连续性的有限观测切换

网络层可以独立承载 R0/R6 的研究问题，不必作为波束方向失败后的被动退路。一个明确的切入点是：

> **在共享未来轨道信息、有限候选链路测量和相同切换成本下，用结构化环境预测降低实际业务损失。**

### R7.1 系统边界

可从 8–16 个 UE、局部可见卫星集合开始，再扩展到 32 个 UE。单 UE 可用于调试，但不能支撑多用户负载均衡结论。

区分 UE–beam、beam–satellite 和 satellite–gateway。第一版可以把“卫星关联”定义为抽象的服务资源选择，但需说明波束和网关是固定、非瓶颈还是已并入容量。可见不等于可接入；无覆盖时允许断连动作，而不是强制每个 UE 必须连接一颗星。

采用物理上下文 c_t、外生未知量 ξ_t 和可控状态 x_t 的同样分解。控制器可观察服务链路、稀疏候选链路反馈与延迟负载报告，不能免费获得所有星的实时 CSI、队列和可用资源。

### R7.2 保留队列守恒，不重复学习已知规律

若 q_u(t) 以 bit 计，A_u(t) 为本时隙末加入的新业务，S_u(t) 为本时隙可提供服务的 bit 数：

$$
D_u(t)=\min\{q_u(t),S_u(t)\},\qquad
q_u(t+1)=\min\{q_u^{\max},q_u(t)-D_u(t)+A_u(t)\}.
$$

溢出 bit 另外记录为丢失。若有包截止期，需维护年龄/截止期桶或包级时间戳，单一 q 不足以计算 deadline violation。

例如：

$$
S_u(t)=
\Delta_{\mathrm{ctrl}}\,
\eta_u^{\mathrm{HO}}(t)\,
b_u(t)\,
\log_2(1+\mathrm{SINR}_u(t)),
$$

其中 b_u 为分配带宽，η_HO∈[0,1] 为计入切换中断后的可用时隙比例。若使用链路到系统映射，可替换 Shannon 近似。任何重传损失或传输开销都应说明是否计入。

卫星总带宽/时隙分配由已知调度器计算。学习到达、链路 residual 和不可见背景需求；若所有负载都由已知 UE 和调度动作决定，就无需再用网络神秘地“预测 load”。

### R7.3 论文聚焦

动作只包括保持连接/选择合法目标星。候选序列同时影响切换计时器、资源分配和队列。多 UE 动作必须整体评估，不能每个 UE 独立挑最优卫星后忽略共同容量。

首要指标建议使用：

- 同等 handover 次数下的业务交付率、outage 时长或 deadline miss；
- 5% 用户吞吐作为公平性辅助指标；
- 反馈次数/开销、真实决策时延。

**必要对手：** PreHO 类预测规划、load-aware greedy、AR/Kalman/HMM + 相同 MPC、相同观测的递归 model-free RL、黑盒 WM + 相同 planner。简化实现应标为“受某方法启发”，不能直接冠以原算法名称。

若未来外生过程与动作独立，“外生量预测 + 精确排队/资源模型”已足以计算所有动作后果。这是应认真比较的强方法，不能通过删除动作效果来制造一个不合理的“无 action baseline”。


## R8. 决定论文是否站得住的实验

### R8.1 第一优先级：明确 WM 在什么条件下有价值

围绕两个无量纲关系做扫描：

$$
\rho_{\mathrm{age}}=
\frac{\text{观测年龄}}{\text{被预测统计量的变化时间尺度}},
\qquad
\rho_{\mathrm{act}}=
\frac{\text{控制完成时间}}{\text{业务/干扰变化时间尺度}}.
$$

这里分母对应慢变协方差、背景负载或遮挡统计，不自动等于瞬时小尺度信道 coherence time。

预期但未验证的逻辑是：低观测年龄时简单优化可能已足够；中等年龄且变化可学习时预测可能有价值；完全不可预测或观测过于陈旧时，应依赖保守控制。**真正值得画的是收益适用区间，而非只挑一个最有利参数点。**

### R8.2 主方案 A 的最小基线集合

| 方法 | 作用 |
|---|---|
| 当前/最后一次 sCSI 的鲁棒波束设计 | 测试是否仅仅因为用了未来信息而获益 |
| 物理几何预测 + 固定统计 residual | 体现已知轨道和阵列信息本身的价值 |
| AR/Kalman 或 HMM/IMM 预测 + 同一鲁棒 planner | 最重要的简单时序预测对手；模型匹配环境下应很强 |
| GRU 统计预测 + 同一解析 planner | 分离“时序预测有用”和新结构/训练有用 |
| 相同信息、近似参数量的黑盒 WM + 同一 planner | 验证物理结构与输出表示的作用 |
| 最终方法 | 仅加入已被消融证明有效的模块 |
| Oracle 参考 | 使用隐藏当前状态或未来扰动，各自明确特权；不作为可部署对手 |

理想信息不自动等于性能上界：若 oracle 仍使用近似搜索，它只是“理想信息参考”。只有优化过程及目标满足相应条件，才能称严格上界。加入 RL 时，只选适合动作空间的一种合理方法；不要为了表格完整盲目复现 PPO、SAC、DQN、Dreamer 全家桶。

### R8.3 六组证据，控制图数而不削弱结论

1. **控制收益曲线（最重要）。** 有效速率/服务量 vs 平均泄漏违约率，在相同反馈预算下比较 Pareto 前沿；或固定允许违约率后比较速率。不允许用“更频繁违反干扰约束”换取吞吐。
2. **动作后果预测。** 使用仿真分支真值，比较候选波束的干扰预测、候选效用排序和 decision regret；低状态 RMSE 不足以替代这一组。
3. **时序必要性。** 扫描反馈年龄、变化时间尺度、控制延迟以及 H；包含 H=1 与 H>1，扣除重配置/测量成本。
4. **结构消融。** 无物理分解、无二阶结构/PSD 约束、无多步训练、无风险处理。每次只改变被检验因素；若其他组件不可分，则诚实标为整体消融。
5. **泛化/样本效率。** 10%、25%、50%、100% 独立训练 episode；测试未见轨迹/保护点布局与未见反馈延迟。至少包含一种参数偏移和一种机制偏移，而非只换 random seed。
6. **运行成本。** 训练数据量、训练时间、模型大小、推理加搜索的 median/p95 时延；若声称适合星上运行，需要相应设备或可信部署测量，桌面 GPU 结果只支持该硬件下的结论。

若选择联合信息控制为中心命题，主动测量不是附加图，而应成为主实验轴；加入 R6.1 的分解与参照实验。安全方向若成为独立主线，则重新围绕辨识与恢复组织实验，避免用两套不相干的 benchmark 稀释中心结论。

### R8.4 必须有的负对照与失败判据

- **纯确定性环境：** 完整物理优化应接近最优；WM 不应凭空产生显著优势。
- **模型匹配的 AR/HMM 环境：** 给经典估计器相同或估计得到的参数条件，防止只是在击败故意弱化的基线。
- **独立随机未来：** 检验模型是否虚假自信；可预测均值但不能提前知道随机事件。
- **完全新鲜、近似全状态反馈：** 若 WM 优势消失，这是合理的适用边界。
- **无行动作效果/不可行几何：** 若保护点与服务用户的空间可分辨性不足，零陷会同时损伤服务信号；算法不能创造阵列自由度。
- **相位验证：** 在非相干模型允许的公共相位变换下检验不变性；相干条件改变后应明确模型适用性变化。
- **超长 horizon：** 展示误差累积与控制收益可能下降，不将“越长越好”预设为结论。

**探索阶段的证据要求：** 用代表性场景和独立 episode 检查问题是否真实、动作是否有控制能力、观测是否可辨识以及对比是否公平。早期少量 seed 只用于诊断，不作为接受或否定方向的充分证据。不用固定 5%–10% 收益或某一周的截止日期决定选题；应结合机制新意、误差来源、最强基线表现与进一步验证的价值判断。

### R8.5 统计、数据划分与公平性

- 先按完整 episode/轨迹/地理区域分训练、验证、测试，再生成滑动窗口；相邻窗口不能跨集合泄漏。
- normalization、校准阈值、planner 惩罚权重、模型选择只用训练/验证集。测试集冻结。
- 完整实验建议至少 5 个训练 seed，每个模型评估相同的独立测试轨迹；测试数量依实际尾部事件置信区间增加。
- 报告配对 episode 差异的均值与置信区间，时间相关场景用合适的 block/episode 重采样；不能把每个相关 time slot 当独立样本。
- 极少违约时报告事件数、总暴露量和不确定性；“观察到 0 次”不等于真实概率为 0。
- 所有 planner 共享轨道信息、可观测量、动作集合、功率/容量限制、搜索预算与反馈成本。额外预训练数据必须记录。
- 泛化时分清 zero-shot、少量适应和重新训练；单纯换用户数与打乱编号，不能等价于真实环境外推。

### R8.6 关于安全分支的后续实验规范

若将来选择方向 E，需要单独解决两个问题：异常能否被发现、发现后模型能否预测恢复动作。建议正常模型负责变点/异常参考，另用有限扰动假设集、显式干扰状态估计或有数据支持的适应模型规划恢复。对完全未知动态退回已定义的保守策略。

“异常”不等于“攻击”。正常业务激增、遮挡、位置误差也应进入 false-alarm 测试。若只训练 normal、测试突然注入大干扰，得到好看的 AUC 仍不能证明能检测复杂未见攻击。

## R9. 仿真可信度、模型规模与协作接口

### R9.1 复用现有资源，避免重建整个 NTN 平台

你负责物理与协议边界，合作者负责预测表征和学习。最容易拖慢项目的是同时更换仿真器、学习框架、数据格式和控制算法。先在现有平台上固定最小场景。

协议/信道假设可参考 [3GPP TR 38.811 官方条目](https://portal.3gpp.org/desktopmodules/Specifications/SpecificationDetails.aspx?specificationId=3234)。陆地移动卫星传播与 Earth–space 传播分别有 [ITU-R P.681](https://www.itu.int/rec/R-REC-P.681-11-201908-I) 和 [ITU-R P.618](https://www.itu.int/rec/R-REC-P.618-14-202308-I) 可查，但引用标准名不等于已实现它的全部模型。

使用已有工具时保持问题边界：[Hypatia](https://github.com/snkas/hypatia) 可作为 LEO 网络仿真参考，[SNS3](https://github.com/sns3/sns3-satellite) 是 ns-3 卫星模块；是否适合当前波束/干扰假设要按具体模块核对。若已有 Sionna/射线追踪资产，可以离线生成或校准信道与空间残差，不必把昂贵射线追踪塞进每次在线 MPC rollout。

### R9.2 参数的最低说明要求

| 类别 | 必须交代 |
|---|---|
| 轨道/覆盖 | 轨道来源、epoch、传播方式、时间区间、最小仰角、波束覆盖；TLE 不等于测得的通信性能 |
| 频段/终端 | 上下行、载频、带宽、终端与卫星阵列、方向图、极化、功率/EIRP 约定 |
| 链路 | FSPL、噪声功率与噪声系数、遮挡/衰落适用频段、干扰源与功率单位 |
| 时间 | 物理采样、统计平均窗口、控制周期、反馈发生/到达时间、动作生效延迟 |
| 反馈 | 可获得量、量化/噪声/缺失方式、探测开销、控制器实际知识 |
| 业务/控制 | 有效服务定义、重配置中断、功率约束、干扰门限、无法服务的处理 |
| 数据 | episode 构成、外生过程相关性、训练/测试参数、轨迹/区域切分 |

**建议起点而非标准参数：** 用现有阵列规模，1 个受控服务区域、少量 NTN 用户和保护点、8–32 个动作候选、H∈{1,5,10,20}。具体控制周期由要预测的慢变统计决定；如果是秒级切换，不用它声称补偿毫秒级快衰落。

雨衰、遮挡、小尺度衰落的相对重要性依赖频段和终端环境。不要为让 WM “有东西可学”同时加入全部随机项，更不要人为放慢随机信道到模型容易预测的程度。先校验量级、时间相关性、空间相关性和物理可控性。

### R9.3 合作者擅长大模型，应如何发挥

让其负责**共享预测表征与跨场景迁移**，而非自动加入语言模型。可开展：

- 用 UE/保护点/波束 token 组织低维物理与测量信息，处理变长实体集合；
- 在多轨迹、多布局、多业务或反馈模式上预训练；
- 用少量目标场景数据校准 residual/uncertainty；
- 仅在大模型有稳定增益后蒸馏到小模型，以实际 planner latency 评价收益。

建议一个小 GRU/状态空间模型与一个相同任务的轻量 Transformer 同时试验，尽早决定。模型大小、数据量、预训练计算与标签条件都要记录。跨场景预训练若使用测试布局的物理数据，也可能泄漏，应预先声明允许的信息。

GNN 仅在跨实体耦合与规模泛化确有需要时加入；JEPA 仅在表征目标和防塌缩机制明确时加入。使用大型 backbone 不会自动提高 ICC 论文贡献，更多模拟数据也不自动等于真实泛化。

### R9.4 尽早固定两人的接口，允许随科学问题有依据地演进

建议仿真器提供以下接口约定；这是开发建议，当前尚未生成代码：

~~~text
reset(config, episode_seed)
observe(controller_time)                 # 仅已到达的观测、mask 和 age
legal_actions(controller_time)
step(action, measurement_request=None)
snapshot() / restore(snapshot)           # 仅供离线分支评估
evaluate_branch(snapshot, action_sequence, exogenous_trace_id)
~~~

每条 transition 记录：

~~~text
episode_id, measurement_time, arrival_time, control_time
physics_context_available, observation, missing_mask, observation_age
action, valid_action_mask, action_effective_time
delivered_bits_or_rate, interference, power, reconfiguration_cost
hidden_truth_for_training_or_evaluation_only
~~~

原始数据、校准集合、训练配置和图表脚本应可追溯到相同版本。隐藏真值字段不进入 controller observation；否则漂亮的控制曲线可能来自信息泄漏。


## R10. 以证据为验收标准的研究里程碑

时间线降为辅助管理工具。以下阶段不绑定日期、不要求达到某周就冻结架构；每一步以是否解决了关键不确定性为依据。

| 里程碑 | 你的主要任务：NTN/通信 | 合作者主要任务：模型 | 进入下一阶段的证据 |
|---|---|---|---|
| **问题成立** | 明确实际观测、合法动作、时序、物理约束；建立有代表性的困难场景 | 分析隐状态、可辨识性和所需历史；核对紧邻学习工作 | 问题在合理物理假设下存在，不能由已有信息下的简单解析方法直接解决 |
| **贡献定位** | 明确与预测切换、统计波束/鲁棒控制的差异 | 明确与预测器、WM、dual control、主动感知方法的差异 | 用一段话说明新机制；对应可证伪实验，而非只列新模块 |
| **机制验证** | 构造能区分假设的配对场景和统一 planner；验证分支仿真 | 训练轻量模型与必要对手，拆解表征、预测、信息选择和规划的作用 | 关键机制确实改变动作或改善控制，收益不是信息泄漏或预算不对称造成 |
| **理论与方法深化** | 分析几何/反馈可获得性、约束可行性和误差传播 | 深化 belief、任务相关表征、观测价值或跨场景迁移 | 分析能解释算法设计、收益条件或失败情形；不为装饰而加通用定理 |
| **严格验证** | 多布局、轨迹、业务/干扰机制与真实反馈成本；尽可能引入测量校准 | 多 seed、样本效率、校准、迁移、模型/计算预算控制 | 主结论超过合适强基线，适用边界和不确定性清楚 |
| **论文与 Poster** | 物理解释、系统模型、主结果与实际意义 | 方法、学习设置、复现材料与误差分析 | 六页围绕同一中心命题；图、数字、假设可追溯，poster 讲同一个结论 |

两人共同负责科学问题和贡献判断，避免分工变成“仿真交数据、模型交黑盒”。已有代码降低验证成本，擅长大模型的优势应体现在预测表征、迁移或推理能力的实质收益上。

### 如何决定扩展、修改或停止一个方向

- **扩展：** 关键机制已被识别，下一项实验能检验更重要的假设或增强外部有效性。
- **修改：** 未见收益但有明确、可检验的原因，例如观测不可辨识、训练动作覆盖不足、表示目标与控制不一致。
- **停止：** 在合理场景和公平对比下，核心假设被证伪；或与已有方法的区别只剩术语和无必要的复杂度。
- **暂不判断：** 数据、训练或场景验证不足时，先补足证据，不因短期曲线没有超过基线就宣布方向无价值。

11 月 1 日仍为期望节点，按用户预计延期的前提安排；实际投稿日期与格式在投稿准备时依据[官方信息](https://www.comsoc.org/conferences-events/ieee-international-conference-communications-2027)单独确认。这个行政检查不用于压低研究目标，也不再设置压缩投稿路线。

## R11. 六页论文和 Poster 应怎样组织

### R11.1 围绕一个中心命题组织三个研究问题

若选择 R6 的信息控制主线：

- **RQ1：** 什么样的环境表征和反馈历史足以评估候选测量及控制的后果，已知 NTN 几何能减少哪些预测/识别负担？
- **RQ2：** 推演观测结果和后续控制的联合规划，是否在同等反馈、计算和物理约束下超过任务规则、经典 belief-space 方法与普通 learned planner？
- **RQ3：** 表征、模型和决策机制各贡献多少，结论能否在未见几何、反馈条件或任务成本下保持？

若最终选择抗干扰辨识与适应，则把 RQ2 改为“合法观测/干预能否区分需要不同响应的扰动”，RQ3 重点研究未见机制下的适应与恢复。不要把两套中心命题硬装进同一篇六页论文。

### R11.2 三项贡献的候选写法

下面是待验证的贡献模板，最终应依据文献对照、分析与实验重写：

1. 面向有限观测 NTN，构建任务相关的环境表征与观测模型，将已知未来几何、信息到达时序及物理动作响应纳入后果评估，并明确表征适用条件。
2. 提出测量与控制耦合的规划机制，根据不同反馈分支下的后续可执行决策评价测量价值，处理等待、重配置、反馈成本和通信约束。
3. 通过机制分解、强对手与跨场景验证，说明物理结构、学习动态和信息规划各自的作用，建立收益条件与失效边界；若得到有意义的可辨识性或误差分析，以对应结果充实贡献。

第 1–2 项都不能只靠改名成立。一般 WM、dual control 与主动感知已有研究基础，必须说明本文具体改变了什么假设、模型或算法，以及为什么影响通信结果。第三项的实验与分析支撑方法，不替代方法区别。

### R11.3 六页预算

| 内容 | 建议页数 | 必须回答 |
|---|---:|---|
| Introduction + 紧邻工作 | 0.80 | 哪个通信瓶颈，现有方法为什么还不够？ |
| System model + problem | 0.90 | 观测、动作、干扰统计、时延与约束到底是什么？ |
| Proposed method + 简短分析 | 1.50 | 学什么、物理算什么、如何训练与规划？ |
| Evaluation | 2.10 | 公平基线、主要收益、关键消融和适用边界 |
| Conclusion | 0.20 | 经证据支持的结论与一个清楚的限制 |
| References | 0.50 | 紧邻工作、方法基础、物理模型 |
| **合计** | **6.00** | 以实际目标 CFP 再调整 |

建议 4–5 张紧凑图，必要时做子图：

- 图 1：系统与闭环架构，标清已知物理、可观测反馈、隐藏状态和动作。
- 图 2：主 Pareto 曲线——服务收益 vs 干扰违约；B 方向则为业务损失 vs handover。
- 图 3：观测陈旧度/预测 horizon 对控制收益和动作评估误差的影响。
- 图 4：结构消融与泛化/样本效率。
- 图 5（若空间允许）：信息选择的机制案例或跨场景复用结果。若联合信息控制是主线，反馈开销 tradeoff 应进入图 2–3，不放在可有可无的位置。

参数和运行成本尽量用紧凑表格。不要用三张仅展示预测 RMSE 的图挤掉闭环证据，也不要为 poster 的漂亮故事保留论文中无法支撑的安全分支。

### R11.4 中文摘要草案：信息控制主线，尚无实验结果

低轨卫星的未来几何可以预先推算，但有限且延迟的反馈使通信控制面临隐藏环境状态。测量资源有限时，观测最不确定的链路或干扰点未必最有利于后续决策，因为信息的价值还受到反馈到达时间、剩余服务窗口和物理动作约束的影响。本研究拟构建一种面向任务的混合世界模型，结合已知轨道与信号响应，从观测历史中推断环境动态，并推演候选测量结果及其引起的后续控制。以星地频谱共存为验证场景，研究在反馈成本、重配置代价和干扰约束下联合选择测量与波束动作的方法。通过与统计预测、任务相关测量规则及信息感知规划的公平比较，检验物理结构与学习动态如何影响通信收益，并分析其适用条件和泛化边界。

这是候选研究摘要，不是已完成贡献。若最终证据只支持固定测量下的波束控制，应据实缩回题目与摘要；若信息控制机制成立，它应成为正文与主实验核心。

### R11.5 Poster：围绕同一个结论，30 秒能讲明白

建议题目与论文一致。核心句可以是：

> **反馈有限时，先判断哪些信息会改变后续通信决策，再决定测什么、如何控制。**

三栏即可：

| 左栏：问题 | 中栏：机制 | 右栏：证据 |
|---|---|---|
| 一个 NTN–TN 共存场景；有限反馈下，最不确定的对象未必最值得测 | 已知几何 + 隐环境预测；候选测量结果对应不同后续波束；比较整个决策分支 | 同反馈预算的主结果、信息价值的配对案例、计算/反馈成本 |

Poster 主图可先对比“测保护点 A、测保护点 B、暂不测量”，再在每个反馈分支中比较常规波束、窄零陷和功率退避。精简到能看清一项信息如何改变控制；若最终只研究固定测量，则展示三个波束动作及其后果。每个分支展示预测区间与实际分支结果。所有数值来自对应实验，不能为视觉效果编造；初期没有结果时明确标“概念示意”。

30 秒讲稿模板：

> 我们研究有限反馈下的 LEO 星地频谱共存。轨道虽然可预测，哪些环境信息值得测量仍取决于后续通信决策。我们用混合世界模型推演候选反馈及其引起的控制变化，把测量开销和未来通信收益一起考虑。这里的主图展示同预算下相对强基线的结果；另一张图解释为什么某项测量有价值，以及这种判断何时失效。

最终替换“实际改善”为真实数字，附明确基线名称。尺寸、横竖版、机构标识和二维码待具体 poster 活动要求确定；不影响现在完成内容与结果图设计。

## R12. 现在最值得做的事，以及停止加模块的条件

### 首轮应形成的研究证据

1. 一页场景说明：选择下行共存还是接收抗干扰；实际反馈是什么；干扰平均窗口和控制周期是什么。
2. 两张物理 sanity-check 图：预测几何/信道统计随时间变化；不同候选动作确实产生不同的干扰—服务 tradeoff。
3. 一份小规模、可分支复现的数据：包含不同行为策略、缺失 mask、测量年龄和隐藏真值隔离。
4. 一个强经典基线：统计预测 + 当前最好的鲁棒控制；不要只实现 max-SNR。
5. 一个小 GRU 混合模型：先做多步动作后果预测，再接相同 planner。
6. 初始闭环对比与失败/负对照，结合配对场景分析 A、B 或抗干扰路线的机制；不凭一张短期曲线决定最终选题。
7. 一张与最接近 3–5 篇论文的逐项差异表：状态/观测、动作、物理结构、学习目标、风险定义、实验条件。不能只列“是否使用 world model”。

### 应继续投入的迹象

模型改善的不只是 RMSE，还包括动作排序或约束预测；这些改善能在同等信息和预算下转化为实际控制收益；收益在独立测试中存在，并能解释为何与 NTN 的物理或测量特点有关。

### 应收缩或转向的迹象

- 仿真器本身已完整给出未来，模型仅在近似一个廉价解析式；
- 简单 AR/Kalman/HMM + 优化已经同样好，复杂模型只有更长训练时间；
- 所有增益都依赖不现实的免费全状态观测、超强可预测干扰或忽略控制开销；
- 平均吞吐提高，但干扰违约/尾部业务损失同时变坏；
- 需要同时加入 GNN、JEPA、RL、在线适应才能勉强讲清收益来源；
- 无法说明所用反馈在部署时从哪里来。

**当前工作假说：最值得深入的是有限观测下任务相关的环境表征、信息获取与控制之间的关系。** 二阶统计波束控制、预测切换和可辨识扰动下的恢复都可以承载这个问题；最终选择取决于研究命题的重要性、方法区别和可复现证据。时间节点与现有代码都不替代这三个判断。

---

# 历史原稿：第 0–63 节（完整保留）

> 以下是修订前的 brainstorming 原文，作为长期想法库保留，**不代表当前第一篇论文的确定范围或已成立结论**。涉及 DT/WM 定义、未来物理真值、正常模型恢复未知攻击、JEPA/不确定性保证、仿真单位、文献定位及优先级时，以前面的 R0–R12 为准。
> 历史英文摘要和“预期”“最终推荐”均未经过新实验验证；引用也应结合 R2 的更新阅读。


# WorldNTN：基于 Physics-Grounded World Model 的自主与韧性非地面网络研究框架

> **核心定位**：不把 LLM 作为必要组件。研究的技术核心是一个 **physics-grounded、action-conditioned、uncertainty-aware 的 NTN World Model**，利用 LEO 轨道与几何的确定性先验，只学习网络中真正不确定、难以解析建模的动态；同一个 World Model 同时服务于 **预测式网络编排（Predictive Orchestration）** 与 **异常检测/自主防御（Resilience & Security）**。

---

## 0. 一句话版本

传统 NTN Digital Twin 可以根据星历/轨道较容易地预测卫星位置、可见性、距离、仰角、名义 Doppler 和 FSPL，因此这些内容本身不应成为主要创新点。

本项目要研究的是：

> **Can an NTN learn a physics-grounded world model of its own dynamics, use the model to imagine the consequences of candidate actions before executing them, and detect/recover from abnormal conditions when the real network deviates from the predicted world?**

换成中文：

> **能否让 NTN 在确定性轨道物理模型之上，学习一个能够预测“网络如何演化、动作会造成什么后果”的 World Model，并利用这个模型完成主动资源编排、异常检测和自主恢复？**

---

# 1. 为什么不应该只做 “NTN Digital Twin”

LEO NTN 中相当多的未来状态本来就是高度可预测的。

给定卫星星历、UE 位置和时间，可以直接计算或高精度估计：

$$
\mathbf p_s(t),\quad
d_{u,s}(t),\quad
\theta_{u,s}(t),\quad
\phi_{u,s}(t),
$$

进一步得到：

$$
f_D(t),\quad
\tau(t),\quad
L_{\mathrm{FSPL}}(t),
$$

以及 satellite visibility window。

因此，如果所谓 Digital Twin 只是：

```text
TLE / Ephemeris
      ↓
Orbit Propagator
      ↓
Position / Elevation / Doppler / FSPL
```

技术难度和研究 novelty 都有限。

真正难的是轨道信息无法直接回答的问题，例如：

- 下一段时间的业务流量如何变化；
- 卫星负载和排队如何演化；
- weather / blockage / shadowing 对链路造成多少 residual degradation；
- 多用户、多卫星之间的干扰如何变化；
- 某个 handover / beam / power / association action 执行后，未来 KPI 会怎样变化；
- 当前性能突然下降是正常环境变化、拥塞、故障还是 jamming；
- 如果采取不同 mitigation actions，哪个未来结果最好。

这正是从 **Digital Twin** 转向 **World Model** 的意义。

---

# 2. Digital Twin 和 World Model 在这个项目里的区别

## 2.1 Digital Twin：描述“世界现在是什么”

在本项目中，Digital Twin 更适合作为 **physics backbone**：

$$
\mathcal{T}_{\mathrm{phys}}
$$

输入：

$$
\text{ephemeris},\ \text{UE location},\ t
$$

输出：

$$
c_t =
[
\text{visibility},
d,
\text{elevation},
\text{azimuth},
f_D,
\tau,
L_{\mathrm{FSPL}},
\ldots
].
$$

它主要描述 deterministic / semi-deterministic 状态。

---

## 2.2 World Model：学习“世界在 action 作用下会怎样演化”

World Model 要学习：

$$
p_\theta(s_{t+1:t+H}\mid s_{\le t},a_{t:t+H-1},c_{t+1:t+H}),
$$

其中：

- $s_t$：当前网络状态；
- $a_t$：控制动作；
- $c_t$：由 physics twin 提供的确定性上下文；
- $H$：预测 horizon。

关键区别是 **action-conditioned prediction**：

> 不只是预测“未来会怎样”，而是预测“如果我这样做，未来会怎样”。

例如：

$$
p(R_{t+1:t+H}\mid a_t=\text{stay on Sat A})
$$

和

$$
p(R_{t+1:t+H}\mid a_t=\text{handover to Sat B})
$$

应该不同。

这使 World Model 可以用于 counterfactual rollout，也就是在模型内部尝试多个未来。

---

# 3. 项目总体框架

推荐名称：

## **WorldNTN**
### *A Physics-Grounded World Model for Autonomous and Resilient Non-Terrestrial Networks*

总体结构：

```text
                    ┌───────────────────────────────┐
                    │   Deterministic Physics Twin │
                    │ orbit / geometry / FSPL /    │
                    │ Doppler / visibility         │
                    └──────────────┬────────────────┘
                                   │ c_t
                                   ↓
Network telemetry ───────→ ┌───────────────────────┐
                           │   NTN WORLD MODEL      │
Actions a_t ─────────────→ │                       │
                           │ latent dynamics        │
                           │ KPI prediction         │
                           │ uncertainty            │
                           │ counterfactual rollout │
                           └───────────┬───────────┘
                                       │
                   ┌───────────────────┴───────────────────┐
                   ↓                                       ↓
        Predictive Orchestration                  Resilience / Security
                   │                                       │
        association / handover                   anomaly detection
        beam / power / routing                   fault / jammer detection
        load balancing                           mitigation planning
                   │                                       │
                   └───────────────────┬───────────────────┘
                                       ↓
                                  Physical NTN
                                       │
                                       └──── telemetry feedback
```

这里完全不需要 LLM。

**World Model + Planner + Detector** 就可以形成闭环。

---

# 4. 最核心的研究思想：Physics learns what is known; AI learns what is unknown

建议把整个状态拆成两部分：

$$
s_t = (c_t,x_t).
$$

其中：

## 4.1 Physics context $c_t$

由解析模型直接得到：

$$
c_t =
[
\mathbf p_s,
d_{u,s},
\theta_{u,s},
f_D,
\tau,
L_{\mathrm{FSPL}},
V_{u,s}
].
$$

这里的 $V_{u,s}$ 表示 visibility。

---

## 4.2 Learned network state $x_t$

需要 World Model 学习：

$$
x_t =
[
\text{SINR residual},
\text{traffic},
\text{load},
\text{queue},
\text{interference},
\text{blockage},
\text{fault state},
\ldots
].
$$

因此可以写成：

$$
x_{t+1}
=
F_\theta(
x_{\le t},
c_{\le t+1},
a_t
)
+
\epsilon_t.
$$

或者更直观地写：

$$
y_t
=
y_{\mathrm{physics},t}
+
r_t,
$$

World Model 不重新学习整个 $y_t$，只学习 residual：

$$
r_{t+1}
=
F_\theta(r_{\le t},c_{\le t+1},a_t).
$$

这比 black-box 网络更合理，也更容易泛化。

---

# 5. 推荐的具体技术方案：Physics-Grounded Graph Latent World Model

我最推荐的不是普通 LSTM，也不是直接套一个大 Transformer，而是：

## **PG-GLWM: Physics-Grounded Graph Latent World Model**

原因是 NTN 天然可以表示成动态异构图：

$$
G_t=(V_t,E_t).
$$

---

## 5.1 节点

可以包含：

### Satellite node

特征：

$$
[
\mathbf p_s,
\mathbf v_s,
SOC_s,
load_s,
queue_s,
compute_s
].
$$

最初 MVP 不需要 battery/compute，可以只用：

$$
[
\mathbf p_s,
load_s
].
$$

### UE node

$$
[
\mathbf p_u,
traffic_u,
QoS_u,
serving\ satellite
].
$$

### Gateway node（后期）

$$
[
capacity_g,
load_g,
availability_g
].
$$

### Jammer / interference source

训练时不一定显式作为 node。

如果希望检测未知 jammer，更好的做法反而是：

> 正常训练数据中不存在 jammer node，模型只学习 normal dynamics。

---

## 5.2 边

例如：

- UE–Satellite；
- Satellite–Gateway；
- Satellite–Satellite ISL。

UE–Satellite edge feature：

$$
e_{u,s,t} =
[
d_{u,s},
\theta_{u,s},
L_{\mathrm{FSPL}},
f_D,
SINR,
R_{u,s},
visibility
].
$$

其中 deterministic 的部分来自 physics engine。

---

# 6. World Model 的内部结构

推荐四个模块：

```text
G_t + Physics c_t
       ↓
┌─────────────────┐
│ Graph Encoder   │
└────────┬────────┘
         ↓
        z_t
         │
a_t ─────┼─────────────┐
         ↓             │
┌───────────────────┐  │
│ Latent Dynamics   │  │
│ Model             │  │
└────────┬──────────┘  │
         ↓             │
   ẑ_{t+1:t+H}         │
         │             │
 ┌───────┴────────┐    │
 ↓                ↓    ↓
KPI Decoder   State Decoder   Reward/Cost Head
```

---

## 6.1 Graph Encoder

用 heterogeneous GNN / graph attention network：

$$
z_t = E_\phi(G_t,c_t).
$$

它将规模较大的 NTN 图压缩为 latent representation。

如果网络规模较小，第一版甚至可以不做全图 latent，只保留每个 UE 周围的 local subgraph。

---

## 6.2 Action-conditioned latent dynamics

核心：

$$
\hat z_{t+1}
=
F_\theta(z_t,a_t,c_{t+1}).
$$

多步 rollout：

$$
\hat z_{t+k}
=
F_\theta(
\hat z_{t+k-1},
a_{t+k-1},
c_{t+k}
).
$$

这里 $c_{t+k}$ 不需要预测，因为 orbit 可以直接计算。

这是 NTN 相比普通 mobile network 一个非常大的优势：

> **World Model 不需要浪费 capacity 学卫星运动，只需学习 physics-conditioned residual dynamics。**

---

# 7. Latent Dynamics 应该选什么 AI 方法

可以按照难度分三档。

## 方案 A：Graph Encoder + GRU/LSTM

最容易做。

$$
z_{t+1}=\mathrm{GRU}(z_t,a_t,c_{t+1}).
$$

优点：

- 训练稳定；
- 快；
- 很适合 first prototype。

缺点：

- “World Model”味道稍弱；
- 长 horizon prediction 可能容易 drift。

建议作为 baseline。

---

## 方案 B：Graph Recurrent State-Space Model（推荐主模型）

可以做成类似 RSSM 的 stochastic latent dynamics：

$$
h_{t+1}
=
f(h_t,z_t,a_t,c_{t+1}),
$$

$$
p(z_{t+1}\mid h_{t+1})
=
\mathcal N(\mu_\theta,\Sigma_\theta).
$$

observation encoder 提供 posterior：

$$
q(z_{t+1}\mid o_{t+1},h_{t+1}).
$$

这样模型天然具有：

- uncertainty；
- latent rollout；
- model-based planning；
- anomaly likelihood。

这是我认为最适合完整 paper 的版本。

可以把它称为：

### **Physics-Conditioned Graph RSSM**

---

## 方案 C：Graph-JEPA / Predictive Representation World Model

这是更新、也更有“AI-native 6G”味道的做法。

不是强迫模型重建所有 telemetry：

$$
\hat o_{t+1}\approx o_{t+1},
$$

而是预测 future latent embedding：

$$
\hat z_{t+k}
=
F_\theta(z_{\le t},a,c),
$$

并让：

$$
\hat z_{t+k}\approx
\mathrm{stopgrad}(E(o_{t+k})).
$$

优势：

1. 不需要精确重构所有噪声；
2. 学到对控制有意义的 representation；
3. 特别适合 anomaly detection；
4. 可以做 self-supervised pretraining。

建议作为 **第二阶段/更有 novelty 的模型**。

---

# 8. 为什么不建议第一版用 Diffusion World Model

Diffusion 可以建模复杂多模态未来：

$$
p(s_{t+1:t+H}\mid s_t,a_t).
$$

但它用于实时 NTN control 有明显问题：

- rollout 成本高；
- candidate actions 多时采样次数会快速增加；
- latency 不容易控制。

因此它比较适合作为未来增强，而不是第一版。

第一篇更建议：

> **Graph-RSSM / Graph-JEPA + Model Predictive Control**

---

# 9. World Model 训练目标

建议不要只有 single-step MSE。

总 loss 可以写成：

$$
\mathcal L
=
\lambda_1\mathcal L_{\mathrm{KPI}}
+
\lambda_2\mathcal L_{\mathrm{latent}}
+
\lambda_3\mathcal L_{\mathrm{rollout}}
+
\lambda_4\mathcal L_{\mathrm{unc}}
+
\lambda_5\mathcal L_{\mathrm{phys}}.
$$

---

## 9.1 KPI prediction loss

预测：

- SINR；
- throughput；
- satellite load；
- queue；
- outage probability。

例如：

$$
\mathcal L_{\mathrm{KPI}}
=
\sum_{k=1}^{H}
w_k
\|
\hat y_{t+k}-y_{t+k}
\|_2^2.
$$

---

## 9.2 Latent prediction loss

如果使用 JEPA：

$$
\mathcal L_{\mathrm{latent}}
=
\sum_{k=1}^{H}
\|
\hat z_{t+k}
-
\mathrm{sg}(z_{t+k})
\|_2^2.
$$

---

## 9.3 Multi-step rollout loss

需要特别防止：

> single-step prediction 很准，但 rollout 20 步后崩掉。

因此训练时直接做：

$$
\hat s_{t+1:t+H}
$$

并计算 multi-step loss。

---

## 9.4 Uncertainty loss

如果输出 Gaussian：

$$
p(y_{t+k})
=
\mathcal N(
\mu_{t+k},
\Sigma_{t+k}
),
$$

则可以使用 NLL：

$$
\mathcal L_{\mathrm{unc}}
=
-\log
p_\theta(y_{t+k}).
$$

这对安全检测很重要，因为 anomaly 不能只看绝对误差，而应该考虑：

> 这个误差相对于模型本身的 uncertainty 是否异常。

---

## 9.5 Physics consistency loss

例如模型输出的 path-loss residual 不应该重新破坏 deterministic geometry trend。

可以加入：

$$
\mathcal L_{\mathrm{phys}}
=
\|
\hat y_t
-
(
y_{\mathrm{physics},t}
+
\hat r_t
)
\|^2.
$$

或者对某些完全 deterministic 的 state 直接不预测。

**最好的 physics constraint 是 architectural constraint，而不是仅仅加 loss。**

也就是说：

> 已知的 orbit / geometry 直接输入未来真值，不让 neural network 预测。

---

# 10. Branch A：Predictive NTN Orchestration

这是第一条应用主线。

World Model 的作用不是直接输出 action，而是提供：

$$
\text{candidate action}
\rightarrow
\text{predicted future}.
$$

---

## 10.1 最适合第一版的问题：Satellite Association + Handover

定义：

$$
x_{u,s}[t]\in\{0,1\},
$$

表示 UE $u$ 在时刻 $t$ 是否连接 satellite $s$。

约束：

$$
\sum_s x_{u,s}[t]=1.
$$

如果需要容量约束：

$$
\sum_u x_{u,s}[t]R_u[t]
\le C_s[t].
$$

---

## 10.2 目标函数

例如：

$$
J
=
\sum_{\tau=t}^{t+H}
\left[
\sum_u R_u[\tau]
-
\lambda_{\mathrm{HO}}N_{\mathrm{HO}}[\tau]
-
\lambda_{\mathrm{out}}N_{\mathrm{out}}[\tau]
-
\lambda_{\mathrm{bal}}L_{\mathrm{imbalance}}[\tau]
\right].
$$

然后不是用真实网络测试所有 action，而是在 World Model 中：

$$
\hat J(a_{t:t+H-1})
=
\mathbb E_{\mathrm{WM}}[J].
$$

选择：

$$
a^*
=
\arg\max_a
\hat J(a).
$$

---

# 11. Planner 不需要 LLM，也不一定需要 RL

这是非常重要的一点。

World Model 和 planner 可以完全分离。

推荐：

## **Model Predictive Control (MPC)**

每个时间 $t$：

1. 读取 real telemetry；
2. 更新 latent state $z_t$；
3. 枚举/采样候选 actions；
4. World Model rollout；
5. 计算未来 utility；
6. 选择最优 action；
7. 只执行第一步；
8. 下一时隙重新规划。

即：

$$
a_t^*
=
\arg\max_{a_t}
\mathbb E
\left[
\sum_{k=0}^{H-1}
\gamma^k r_{t+k}
\right].
$$

---

## 11.1 action space 小时

直接 enumerating top-K candidate satellites。

例如每个 UE 只考虑：

$$
K=3\sim 8
$$

个最有希望的 visible satellites。

---

## 11.2 action space 大时

可以使用：

- Cross-Entropy Method (CEM)；
- beam search；
- Monte-Carlo Tree Search；
- learned proposal policy + World Model evaluation。

第一版推荐 CEM 或 beam search。

---

# 12. 为什么这个方法比普通 DRL 更有研究价值

传统 model-free DRL：

$$
s_t
\rightarrow
\pi_\phi(a_t|s_t).
$$

它直接记 policy。

World Model：

$$
(s_t,a_t)
\rightarrow
p(s_{t+1},r_{t+1}).
$$

因此同一个模型可以：

- 换 objective；
- 做不同 counterfactual；
- 检测 anomaly；
- 解释 action consequence；
- 在攻击状态下重新规划。

所以 World Model 是一个 **reusable environment intelligence layer**，而不是一个 task-specific handover policy。

---

# 13. Branch B：World-Model-Based Anomaly Detection

这部分可以和 orchestration 共用同一个模型。

这是整个大框架最漂亮的地方。

World Model 在 normal network 上学习：

$$
p_\theta(o_{t+1}\mid o_{\le t},a_t,c_{t+1}).
$$

实际网络返回：

$$
o_{t+1}^{\mathrm{real}}.
$$

如果现实和 predicted world 明显不同：

$$
o_{t+1}^{\mathrm{real}}
\not\sim
p_\theta(o_{t+1}),
$$

则认为存在 abnormal condition。

---

# 14. Anomaly Score

可以设计成三个分量：

## 14.1 KPI residual

$$
A_t^{\mathrm{KPI}}
=
(
y_t-\hat\mu_t
)^T
\hat\Sigma_t^{-1}
(
y_t-\hat\mu_t
).
$$

这比普通 MSE 更合理，因为考虑 uncertainty。

---

## 14.2 Latent residual

$$
A_t^{\mathrm{latent}}
=
\|
z_t^{\mathrm{obs}}
-
\hat z_t^{\mathrm{pred}}
\|^2.
$$

特别适合 JEPA。

---

## 14.3 Likelihood score

$$
A_t^{\mathrm{NLL}}
=
-\log p_\theta(y_t|h_{t-1},a_{t-1},c_t).
$$

最终：

$$
A_t
=
\alpha A_t^{\mathrm{KPI}}
+
\beta A_t^{\mathrm{latent}}
+
\gamma A_t^{\mathrm{NLL}}.
$$

当：

$$
A_t>\eta
$$

触发 anomaly。

---

# 15. 为什么非常适合 jamming detection

正常情况下，geometry 可能预测：

$$
\Delta L_{\mathrm{FSPL}}
\approx 0.3\ {\rm dB}.
$$

但实际 SINR 突然下降：

$$
\Delta SINR=-12\ {\rm dB}.
$$

同时 satellite load 和 traffic 没有相应变化。

那么这一 observation 与 learned normal dynamics 的 likelihood 很低。

这意味着：

> “轨道物理无法解释当前变化，正常网络 dynamics 也无法解释。”

模型不需要一开始就知道 “这是 jammer”。

它首先只需要知道：

$$
\text{This state is abnormal.}
$$

---

# 16. 第一版安全研究最好先做 “Detection”，而不是强行做 Attack Classification

建议训练数据只包括 normal state。

测试加入：

- jammer；
- sudden blockage；
- traffic surge；
- satellite failure。

先回答：

> World Model 能不能检测 unseen abnormal dynamics？

这比训练一个：

$$
\text{normal/jammer classifier}
$$

更有研究意义，因为 supervised classifier 依赖 attack labels。

---

# 17. 第二阶段：Root Cause / Diagnosis

如果第一阶段成功，再研究：

$$
H_0=\text{normal}
$$

$$
H_1=\text{jamming}
$$

$$
H_2=\text{blockage}
$$

$$
H_3=\text{traffic congestion}
$$

$$
H_4=\text{satellite/gateway failure}.
$$

不需要 LLM，可以用两种方法。

---

## 方法 A：Structured Hypothesis Models

每个 hypothesis 有一个扰动 model：

$$
p(o_t|H_i).
$$

选择：

$$
H^*
=
\arg\max_i
p(o_t|H_i).
$$

---

## 方法 B：Small diagnosis head

World Model encoder freeze 后：

$$
z_t\rightarrow
\text{fault classifier}.
$$

只需要少量 attack labels。

这样可以形成：

> self-supervised normal-state pretraining + few-shot diagnosis。

---

# 18. Branch C：World-Model-Based Autonomous Recovery

Detection 以后，最关键的是不要使用固定 response：

```text
jammer detected → always handover
```

而是在 World Model 中测试 mitigation。

例如：

$$
\mathcal A_{\mathrm{mit}}
=
\{
\text{handover},
\text{beam nulling},
\text{power control},
\text{frequency switch},
\text{TN fallback}
\}.
$$

对每个 action：

$$
a_i
\rightarrow
\mathrm{WM}
\rightarrow
\hat s_{t+1:t+H}^{(i)}.
$$

计算：

$$
J_i
=
R_i
-
\lambda_1 P_{\mathrm{out},i}
-
\lambda_2 C_{\mathrm{HO},i}
-
\lambda_3 P_{\mathrm{TX},i}.
$$

然后：

$$
a^*
=
\arg\max_i J_i.
$$

这就形成：

$$
\boxed{
Predict
\rightarrow
Detect
\rightarrow
Recover
}
$$

---

# 19. 最重要的统一视角

Orchestration 和 Security 不是两个拼在一起的项目。

它们本质上是同一个机制：

## 正常情况下

$$
\text{World Model}
+
\text{counterfactual rollout}
\rightarrow
\text{best control action}.
$$

## 异常情况下

$$
\text{prediction mismatch}
\rightarrow
\text{anomaly}
\rightarrow
\text{counterfactual rollout}
\rightarrow
\text{best mitigation action}.
$$

可以在论文里概括为：

> **The same predictive model that tells the network what should happen under normal operation also provides the reference needed to recognize when the real system no longer behaves as expected.**

这应该成为整篇工作的核心 narrative。

---

# 20. 推荐的第一版系统模型

为了在较短时间内做出完整结果，第一版不要做整个 mega-constellation。

推荐：

- 一个有限地理区域；
- $N_u=8\sim 32$ 个 UE；
- 每个 UE 同时看到若干候选 LEO satellites；
- time slot：0.5–2 s；
- episode：2–10 min；
- horizon：5–30 steps；
- satellite motion：SGP4 / orbital propagator；
- channel：FSPL + shadowing / weather residual；
- traffic：AR / bursty / Markov-modulated traffic；
- jammer：固定或移动 terrestrial jammer。

第一版只做：

$$
\boxed{
association + handover + jamming
}
$$

后面再扩展 beamforming。

---

# 21. Environment 的具体状态定义

对于 UE $u$、satellite $s$：

$$
s_t =
\{
c_t,\,
q_u,\,
L_s,\,
I_{u,s},\,
SINR_{u,s},\,
R_{u,s},\,
x_{u,s}
\}.
$$

其中 physics context：

$$
c_t=
\{
\theta_{u,s},
d_{u,s},
f_{D,u,s},
L_{\mathrm{FSPL},u,s},
T_{\mathrm{remain},u,s}
\}.
$$

推荐加入一个非常 NTN-specific 的特征：

$$
T_{\mathrm{remain},u,s}
$$

即剩余可服务时间。

---

# 22. Action

第一阶段：

$$
a_t=x_{u,s}[t].
$$

也就是 association。

第二阶段：

$$
a_t=
[
x_{u,s},
P_s
].
$$

第三阶段：

$$
a_t=
[
x_{u,s},
P_s,
w_s,
f_s
].
$$

其中 $w_s$ 为 beamforming action。

---

# 23. Traffic Model

为了让 World Model 真正有东西可以学习，不应让整个 simulator 都 deterministic。

至少加入：

$$
\lambda_u(t)
=
\mu_u(t)+\epsilon_t.
$$

可以使用：

### AR(1)

$$
\lambda_{t+1}
=
\rho\lambda_t
+
(1-\rho)\mu
+
\epsilon_t.
$$

或 Markov-modulated Poisson process。

后面再使用真实 traffic trace。

---

# 24. Channel Residual

可以：

$$
L_{u,s}(t)
=
L_{\mathrm{FSPL}}(t)
+
L_{\mathrm{weather}}(t)
+
L_{\mathrm{shadow}}(t)
+
L_{\mathrm{misc}}(t).
$$

World Model 已知：

$$
L_{\mathrm{FSPL}}(t),
$$

只学习：

$$
r_t
=
L_{\mathrm{weather}}
+
L_{\mathrm{shadow}}
+
L_{\mathrm{misc}}.
$$

这就是 clean physics-grounded formulation。

---

# 25. Jammer Model

第一版可以很简单。

jammer 到 UE：

$$
P_J^{rx}(t)
=
P_J
+
G_J
+
G_{UE}
-
L_J(t).
$$

SINR：

$$
SINR_{u,s}
=
\frac{
P_{s,u}
}{
I_{network}
+
P_J^{rx}
+
N_0B
}.
$$

训练 World Model 时：

$$
P_J=0.
$$

测试时随机加入：

$$
P_J>0.
$$

这样可以严格测试：

> **unseen anomaly detection**。

---

# 26. 第一组实验：World Model 本身是否预测准确

需要先证明：

> World Model 真的学会 network dynamics。

指标：

### SINR prediction

$$
RMSE_{\mathrm{SINR}}(H)
$$

### Throughput prediction

$$
RMSE_R(H)
$$

### Load prediction

$$
RMSE_L(H)
$$

### Calibration

例如：

$$
P(
y_{t+k}
\in
[\mu-2\sigma,\mu+2\sigma]
).
$$

画：

> prediction error vs prediction horizon。

---

# 27. 第二组实验：Predictive Orchestration

比较：

## Baseline 1：Max-SNR

$$
s^*
=
\arg\max_s SINR_s(t).
$$

## Baseline 2：Max remaining visibility

## Baseline 3：Load-aware greedy

## Baseline 4：Optimization with perfect deterministic orbit only

也就是知道未来 geometry，但不知道 future traffic/interference residual。

## Baseline 5：Model-free RL

例如 PPO / SAC / DQN。

## Proposed

$$
\text{Physics-Grounded WM + MPC}.
$$

---

# 28. Orchestration Metrics

至少：

### Average throughput

$$
\bar R
$$

### 5th percentile throughput

$$
R_{5\%}
$$

### Outage probability

$$
P_{\mathrm{out}}.
$$

### Number of handovers

$$
N_{\mathrm{HO}}.
$$

### Load imbalance

例如：

$$
\sigma_L
$$

或 Jain's fairness index。

### Planning latency

$$
T_{\mathrm{decision}}.
$$

---

# 29. 第三组实验：Anomaly / Jamming Detection

测试不同：

- JNR；
- jammer distance；
- jammer duty cycle；
- burst jammer；
- unseen jammer power；
- multiple jammers。

---

# 30. Security Baselines

## Baseline 1：Energy threshold

## Baseline 2：SINR drop detector

$$
SINR_t-SINR_{t-1}<-\eta.
$$

## Baseline 3：Autoencoder

normal reconstruction error。

## Baseline 4：LSTM predictor

prediction residual。

## Baseline 5：Graph predictor without physics grounding

## Proposed

$$
\text{Physics-Grounded World Model anomaly score}.
$$

---

# 31. Detection Metrics

必须做：

$$
P_D
$$

$$
P_{FA}
$$

ROC / AUC。

另外非常建议：

### Detection delay

$$
T_{\mathrm{detect}}.
$$

### Detection vs JNR

$$
P_D(JNR).
$$

### False alarm under normal distribution shift

例如：

- traffic surge；
- weather change；
- high-load state。

这个非常重要，因为真正困难的不是检测一个很强 jammer，而是：

> **不要把正常网络变化误判成 attack。**

---

# 32. 第四组实验：Autonomous Recovery

攻击出现以后比较：

### No mitigation

### Always handover

### Always power boost

### Rule-based mitigation

### World-Model planning

指标：

- post-attack throughput；
- outage duration；
- recovery time；
- extra power；
- handover cost；
- mitigation success rate。

---

# 33. 最重要的 Ablation Study

这是论文能不能站住的关键。

---

## Ablation A：No Physics

World Model 自己学习所有 dynamics。

对比：

$$
\text{Black-box WM}
$$

vs

$$
\text{Physics-grounded WM}.
$$

期待：

- 训练更快；
- data efficiency 更高；
- OOD orbit / UE geometry 泛化更好。

---

## Ablation B：Physics Only

只有 Digital Twin：

$$
\text{orbit + FSPL + deterministic prediction}
$$

没有 learned residual。

证明：

> Digital Twin 可以准确描述 geometry，但无法充分预测 traffic / interference / anomaly。

---

## Ablation C：No Action Conditioning

模型预测：

$$
p(s_{t+1}|s_t)
$$

而不是：

$$
p(s_{t+1}|s_t,a_t).
$$

如果后者在 planning 明显更好，就证明：

> World Model 的价值不是普通 forecasting。

---

## Ablation D：Single-Step vs Multi-Step Training

证明 rollout loss 对 MPC 的重要性。

---

## Ablation E：Deterministic vs Uncertainty-Aware

对 anomaly detection 尤其重要。

---

## Ablation F：No Graph

将所有 features flatten 后用 MLP/Transformer。

证明 graph inductive bias 在动态 NTN topology 中有价值。

---

# 34. OOD Generalization：非常值得作为 paper 的亮点

World Model 最容易被 reviewer 问：

> “是不是只是在 simulator distribution 上拟合？”

因此必须测试 OOD。

例如：

### OOD-1：不同 satellite orbital plane

### OOD-2：不同 UE density

### OOD-3：不同 traffic distribution

### OOD-4：不同 jammer power/location

### OOD-5：部分 satellite failure

### OOD-6：训练没见过的 weather / shadowing intensity

如果 physics-grounded model 的泛化比 black-box 好，这会成为很强的结果。

---

# 35. Data Efficiency 实验也很有价值

训练数据只使用：

$$
10\%,25\%,50\%,100\%.
$$

比较：

$$
\text{Physics-grounded WM}
$$

vs

$$
\text{Black-box WM}.
$$

如果前者在少量数据下仍能表现好，就能支持一个非常强的观点：

> NTN 的 deterministic orbital structure 可以作为强 inductive bias，降低 world-model learning 的数据需求。

---

# 36. 推荐的最终模型版本

如果目标是 **poster + 后续完整 paper**，我建议最终采用：

## **Physics-Conditioned Heterogeneous Graph RSSM**

### Encoder

$$
E_\phi(G_t)
\rightarrow
z_t.
$$

### Deterministic hidden state

$$
h_{t+1}
=
f_\theta(
h_t,
z_t,
a_t,
c_{t+1}
).
$$

### Prior

$$
p_\theta(z_{t+1}|h_{t+1}).
$$

### Posterior

$$
q_\phi(z_{t+1}|h_{t+1},o_{t+1}).
$$

### KPI decoder

$$
p(y_{t+1}|z_{t+1},h_{t+1}).
$$

### Utility head

$$
\hat r_{t+1}
=
R_\psi(z_{t+1},h_{t+1}).
$$

这样一个模型就能支持：

- stochastic prediction；
- uncertainty；
- multi-step imagination；
- MPC；
- likelihood-based anomaly detection。

---

# 37. 第二个更有 AI novelty 的版本

如果第一版结果好，可以进一步改成：

## **Physics-Grounded Graph-JEPA World Model**

训练时：

```text
Past NTN Graph
      ↓
Context Encoder
      ↓
     z_t
      │
action + future physics
      ↓
Predictor
      ↓
  ẑ_{t+k}
      ↑
Future NTN Graph
      ↓
Target Encoder
      ↓
   z_{t+k}
```

loss：

$$
\|
\hat z_{t+k}
-
\mathrm{sg}(z_{t+k})
\|^2.
$$

随后：

- lightweight KPI head；
- anomaly residual；
- MPC planning。

这个版本可能比普通 RSSM 更容易和最新 “predictive representation / AI-native 6G” literature 对接。

---

# 38. 为什么 Graph-JEPA 对 Security 很自然

训练时只看 normal network：

$$
z_t
\rightarrow
\hat z_{t+1}.
$$

正常测试：

$$
\hat z_{t+1}
\approx
z_{t+1}.
$$

攻击时：

$$
\|
\hat z_{t+1}
-
z_{t+1}^{attack}
\|
\gg 0.
$$

因此 representation prediction 本身就是 anomaly detector。

不需要：

- 重建所有 I/Q；
- 大量 attack labels；
- 专门训练 jammer classifier。

---

# 39. 研究贡献可以如何写

最终论文可以明确写成四个 contribution。

## Contribution 1 — Physics-Grounded NTN World Model

提出一个 NTN-specific World Model，将 deterministic orbital/geometry dynamics 从 learned dynamics 中显式分离：

$$
\text{known physics}
+
\text{learned residual dynamics}.
$$

---

## Contribution 2 — Action-Conditioned Predictive Orchestration

World Model 学习：

$$
p(s_{t+1:t+H}|s_t,a_t),
$$

并通过 counterfactual rollout + MPC 实现 proactive satellite association / handover。

---

## Contribution 3 — Unified Normal-State Anomaly Detection

利用 predicted network evolution 作为 “normal world reference”，通过 uncertainty-aware prediction residual 检测 jamming / fault / unforeseen interference。

---

## Contribution 4 — Model-Based Autonomous Recovery

检测异常后，World Model 对候选 mitigation actions 进行 imagined rollout，选择性能最优的恢复策略。

---

# 40. 什么地方是真正的 Novelty，什么地方不是

## 不足以单独构成 novelty

- 使用 SGP4 建卫星 Digital Twin；
- 根据 orbit 预测 handover；
- 用 GNN 做 satellite association；
- 用 autoencoder 检测 jammer；
- 用 Transformer 预测 SINR；
- 用 DRL 做 handover。

这些方向目前都已经有大量或正在快速增长的工作。

---

## 真正应该强调的组合

$$
\boxed{
NTN\ deterministic\ physics
+
action\text{-}conditioned\ learned\ world\ dynamics
+
counterfactual\ planning
+
normal\text{-}state\ anomaly\ detection
}
$$

最强的 narrative 是：

> **一个统一 World Model，同时成为 network planner 和 normality model。**

---

# 41. 与当前文献的关系和研究空白

截至 2026 年 8 月，几个非常相关的方向已经出现，因此论文一定要避开“只是换名字”的风险。

## 41.1 Telecom World Models

2026 年的 *Telecom World Models: Unifying Digital Twins, Foundation Models, and Predictive Planning for 6G* 已经提出：

- action-conditioned dynamics；
- uncertainty-aware prediction；
- model-based rollout；
- telecom world model architecture。

因此：

> “把 World Model 用于 telecom” 本身已经不是 novelty。

你的区别必须是 **NTN-specific physics structure + unified resilience**。

---

## 41.2 Wireless World Model

*A Wireless World Model for AI-Native 6G Networks* 使用 joint-embedding predictive architecture 和多模态 Transformer，把 CSI、3D point cloud 和 trajectory 融合起来学习 wireless propagation。

所以：

> “World Model 学 channel” 也不是足够独特。

你的 focus 应该是：

> **network-level state/action dynamics，而不是只做 channel foundation model。**

---

## 41.3 Physics-Informed NTN Digital Twin

2026 年已经有工作将 satellite orbital dynamics、channel、weather 和 traffic prediction 放进 physics-informed NTN Digital Twin。

因此：

> “Physics-informed NTN Digital Twin” 本身也不能作为唯一 contribution。

我们的下一步是：

$$
\text{Physics DT}
\rightarrow
\text{Action-conditioned World Model}
\rightarrow
\text{Planning + Resilience}.
$$

---

## 41.4 Predictive LEO Handover

PreHO 已经明确利用 ephemeris 和 LEO 可预测性做 proactive handover planning。

因此不能只说：

> “我们的 World Model 根据未来 satellite trajectory 提前 handover。”

这会被问：

> 为什么需要 AI？Orbit 已经知道。

所以 World Model 必须负责：

- stochastic traffic；
- load；
- interference；
- channel residual；
- abnormal dynamics；
- action consequences。

---

## 41.5 GNN LEO Orchestration

2026 年已有 heterogeneous GNN 用于动态 LEO association / orchestration。

因此 GNN 是 representation tool，而不是核心 novelty。

---

## 41.6 JEPA for AI-Native 6G

近期已有工作明确讨论使用 predictive latent representation 做：

- future network state prediction；
- anomaly detection；
- jamming/spoofing detection。

因此如果使用 JEPA，必须将贡献定位为：

> **NTN-specific physics-grounded graph world model and unified planning/security loop**，

而不是“第一次用 JEPA 检测网络异常”。

---

# 42. 当前最有希望的 research gap

基于上述文献，最值得切入的 gap 是：

> **How can deterministic orbital knowledge be explicitly fused with an action-conditioned learned world model so that a single NTN model can support both proactive control and resilience under unforeseen disturbances?**

三个关键词：

### 1. Physics-grounded

不是 pure data-driven。

### 2. Action-conditioned

不是普通 forecasting。

### 3. Unified planning + anomaly

不是两个独立 AI。

---

# 43. 第一篇 paper / poster 不要做太大

虽然总体框架很大，但第一版只做：

$$
\boxed{
LEO\ association/handover
+
jamming
}
$$

足够。

一个最小闭环：

```text
Orbit / Geometry
       ↓
Physics Context
       ↓
World Model ← telemetry
       ↓
Predict future SINR/load/throughput
       ↓
MPC satellite association
       ↓
Physical simulator
       ↓
prediction mismatch?
       │
      yes
       ↓
anomaly detector
       ↓
simulate mitigation
       ↓
handover / power / nulling
```

---

# 44. Brooklyn AI-Native 6G Summit Poster 可以怎么讲

图中 Summit topic 最匹配：

- **Digital twins and simulation environments**
- **Network automation and intent-based networking**
- **AI for security, privacy, and trust**

不需要强行强调 LLM。

Poster headline 可以是：

## **WorldNTN: A Physics-Grounded World Model for Autonomous and Resilient 6G Non-Terrestrial Networks**

副标题：

> **Predict the network. Detect when reality deviates. Imagine the best action before acting.**

Poster 最好展示三个核心 panel：

### Panel A — Physics + World Model

Deterministic orbit + learned residual dynamics。

### Panel B — Predictive Orchestration

多个 candidate satellites 的 future rollout。

### Panel C — Resilience

现实偏离 prediction → anomaly → mitigation rollout。

---

# 45. Poster 最值得画的一张主图

可以画成：

```text
                      KNOWN PHYSICS
             Orbit / Geometry / Doppler / FSPL
                           │
                           ↓
Telemetry ────────→ ┌───────────────┐ ←──── Candidate actions
                    │   WorldNTN    │
                    │  World Model  │
                    └───────┬───────┘
                            │
                 IMAGINED FUTURE WORLDS
          ┌─────────────────┼──────────────────┐
          ↓                 ↓                  ↓
       Stay A            Switch B          Switch C
       R=...             R=...             R=...
       Outage=...        Outage=...        Outage=...
          └─────────────────┬──────────────────┘
                            ↓
                       Best Action
                            ↓
                        Real Network
                            ↓
                         Telemetry
                            │
                 prediction consistent?
                     /              \
                   yes              no
                    │                │
                 continue       ANOMALY
                                     ↓
                         mitigation rollouts
```

这一张图基本可以把整项工作解释完。

---

# 46. 推荐的结果图

如果时间有限，优先做 5 张。

## Figure 1

**Architecture / framework**

---

## Figure 2

**World-model prediction**

$$
\text{Real KPI vs predicted KPI}
$$

并带 uncertainty band。

---

## Figure 3

**Prediction RMSE vs horizon**

对比：

- physics only；
- black-box predictor；
- proposed。

---

## Figure 4

**Throughput vs handover tradeoff**

对比：

- max-SNR；
- greedy；
- model-free RL；
- WM-MPC。

---

## Figure 5

**Jamming ROC / detection probability**

并加 mitigation 后 throughput recovery。

---

# 47. 推荐开发路线

## Phase 0 — Environment

先建立：

- orbital propagation；
- visibility；
- FSPL；
- traffic；
- association；
- queue/load；
- jammer。

输出 episode 数据：

```text
time
UE state
satellite state
physics features
action
SINR
throughput
load
queue
```

---

## Phase 1 — Prediction baseline

训练：

- MLP；
- GRU；
- Transformer。

目标：

$$
t-K:t
\rightarrow
t+1:t+H.
$$

确认数据中确实存在可学习的 stochastic dynamics。

---

## Phase 2 — Physics-grounded model

未来 orbit 直接输入。

只学习 residual。

与 black-box 模型比较。

---

## Phase 3 — Action conditioning

数据收集时必须探索不同 actions。

否则 World Model 只会看到一个 behavior policy，无法学习：

$$
a\rightarrow outcome.
$$

训练：

$$
p(s_{t+1}|s_t,a_t,c_{t+1}).
$$

---

## Phase 4 — MPC

先不用 RL。

World Model + candidate action rollout。

---

## Phase 5 — Normal-state anomaly detector

只用 normal training data。

测试 jammer。

---

## Phase 6 — Mitigation planning

World Model 评估：

- handover；
- power；
- nulling。

---

## Phase 7 — Graph / JEPA 升级

如果前面结果成立，再追求模型 novelty。

---

# 48. 数据收集时最容易踩的坑

World Model 训练时不能只运行一个 greedy policy。

否则：

$$
a_t
$$

和 state 强相关，模型没有看到足够多 counterfactual transitions。

例如 Sat B 明明 visible，但 greedy policy 从不连接 B。

模型就没有数据学习：

$$
p(s_{t+1}|a_t=B).
$$

因此训练数据要加入 exploration：

- random action；
- epsilon-greedy；
- mixture of policies；
- optimization policy；
- max-SNR；
- load-aware；
- random valid association。

构建更宽的 action support。

---

# 49. World Model 最大的研究风险：Model Exploitation

planner 会找到模型预测错误的区域。

即：

$$
\hat J(a)\gg J_{\mathrm{real}}(a).
$$

它不是找到真正最优 action，而是利用模型漏洞。

解决方法：

## Uncertainty penalty

$$
J_{\mathrm{robust}}
=
\hat J
-
\beta U(a).
$$

其中 $U(a)$ 是 uncertainty。

或者 ensemble：

$$
\{M_1,M_2,\ldots,M_K\}.
$$

选择：

$$
J
=
\mathbb E[J]
-
\beta \mathrm{Var}[J].
$$

这本身甚至可以成为 paper 的一个重要点：

> **uncertainty-aware safe planning in NTN World Models**。

---

# 50. 第二个风险：World Model 可能没有必要

reviewer 会问：

> Orbit 都能预测，为什么不直接 optimization？

所以必须专门设计实验：

### Physics-only planner

拥有 perfect future orbit。

### World-model planner

额外预测：

- traffic；
- load；
- interference；
- residual channel。

如果 WM 显著更好，才能证明必要性。

---

# 51. 第三个风险：Security 看起来像硬拼

解决办法不是把两个独立神经网络放在一个框图里。

一定坚持：

$$
\boxed{
\text{the same world model}
}
$$

既用于：

$$
\text{normal future prediction}
$$

又用于：

$$
\text{normality reference}.
$$

也就是：

$$
\text{prediction error}
\rightarrow
\text{anomaly}.
$$

这样安全分支是 World Model 的自然产物，而不是外挂。

---

# 52. 第四个风险：Simulator-to-reality

第一篇可以接受 simulation，但要为后续留接口。

后续可以逐步引入：

- real TLE；
- real weather；
- measured Starlink / NTN signal traces；
- RF testbed；
- hardware-generated interference。

最重要的是 World Model 本身不依赖某个特定 simulator。

---

# 53. LLM 是否需要？

## 结论：完全不需要。

第一篇甚至建议 **不要加 LLM**。

核心闭环已经完整：

$$
\boxed{
World\ Model
+
MPC
+
Anomaly\ Detector
}
$$

LLM 不会提升：

- channel prediction；
- dynamics modeling；
- anomaly likelihood；
- numerical optimization。

反而会增加：

- architecture complexity；
- latency；
- hallucination；
- reviewer 质疑。

---

# 54. LLM 以后可以放在哪里

如果后面想扩展到 Summit 更明显的 Agentic AI 方向，可以作为 optional layer：

```text
Human Intent
     ↓
LLM Agent
     ↓
objective / constraints
     ↓
World Model + Planner
```

例如：

> Protect emergency users even at the cost of overall throughput.

LLM 将其转换为：

$$
R_u\ge R_{\min},\quad u\in\mathcal U_{\mathrm{critical}}.
$$

但这一层可以完全独立于当前技术核心。

所以 roadmap 是：

$$
\textbf{World Model first, LLM optional later.}
$$

---

# 55. 我最推荐的最终研究题目

### Option A — 最稳

**WorldNTN: Physics-Grounded World Models for Predictive and Resilient Non-Terrestrial Networks**

### Option B — 更强调 autonomous

**WorldNTN: Physics-Grounded Predictive Intelligence for Autonomous 6G Non-Terrestrial Networks**

### Option C — 更强调 security

**Predict, Detect, Recover: A Physics-Grounded World Model for Resilient Non-Terrestrial Networks**

### Option D — 更像 AI conference / modern AI

**Learning the NTN World: Physics-Grounded Latent Dynamics for Predictive Control and Resilience**

当前我最推荐 **Option A**。

---

# 56. 可以直接用于 proposal/poster 的核心 abstract 草案

**Abstract —**  
Low-Earth-orbit non-terrestrial networks (NTNs) exhibit a unique combination of highly predictable physical dynamics and highly uncertain network dynamics. Satellite trajectories, visibility, propagation distance, Doppler shift, and nominal free-space path loss can be accurately derived from orbital information, whereas traffic demand, network load, residual channel variations, interference, and failures remain stochastic and difficult to model analytically. This project proposes **WorldNTN**, a physics-grounded, action-conditioned world model for autonomous and resilient NTN operation. Rather than relearning known orbital physics, WorldNTN uses a deterministic physical twin as structured context and learns only the residual network dynamics needed to predict future key performance indicators under candidate control actions. The learned model enables fast counterfactual rollouts for model-predictive satellite association and handover decisions. The same predictive model also serves as a normality reference: deviations between predicted and observed network evolution provide an uncertainty-aware signal for detecting previously unseen interference, jamming, and failures. Once an anomaly is detected, candidate mitigation actions are evaluated through imagined rollouts before execution. This unified **predict–detect–recover** framework explores how world models can move NTN digital twins beyond passive replication toward predictive control and autonomous resilience.

---

# 57. 最终建议的 research question

主问题：

> **How can deterministic orbital physics and learned network dynamics be combined into an action-conditioned NTN world model that supports both predictive control and autonomous resilience?**

可以拆成四个子问题：

### RQ1

Does explicit physics grounding improve long-horizon prediction and OOD generalization compared with purely data-driven world models?

### RQ2

Can action-conditioned world-model rollouts outperform reactive and model-free policies for satellite association and handover?

### RQ3

Can normal-state world-model prediction residuals detect unseen interference/jamming without attack-specific training?

### RQ4

Can the same model select effective mitigation actions through counterfactual rollout after an anomaly occurs?

---

# 58. 最终建议的研究假设

### H1

Physics grounding reduces sample complexity and rollout error.

### H2

Action-conditioned planning reduces outage/handover tradeoff compared with reactive policies.

### H3

Uncertainty-aware latent prediction residuals can detect unseen jamming with lower false-alarm rate than simple KPI thresholding.

### H4

World-model-based mitigation outperforms fixed rule-based recovery under heterogeneous attacks/disturbances.

---

# 59. 如果只能先完成一个最小 MVP

建议只做：

$$
\boxed{
1\ UE
+
3\sim 8\ visible\ satellites
+
stochastic\ channel/load
+
1\ jammer
}
$$

先验证四件事：

1. Physics + learned residual 比 pure neural predictor 准；
2. action-conditioned model 能预测不同 satellite choice 的未来 throughput；
3. WM-MPC 比 max-SNR 少 outage / handover；
4. jammer 出现时 prediction residual 明显上升。

如果这四件事成立，再扩展多用户、多卫星和 graph。

---

# 60. 如果要直接冲一个更完整的 conference paper

系统规模扩展到：

- 8–32 UEs；
- 10–100 candidate satellites / time window；
- heterogeneous traffic；
- satellite capacity constraint；
- multi-user association；
- random jammer；
- partial satellite failures。

主模型：

$$
\boxed{
Physics\text{-}Conditioned\ Heterogeneous\ Graph\ RSSM
}
$$

控制：

$$
\boxed{
Uncertainty\text{-}Aware\ MPC
}
$$

安全：

$$
\boxed{
Latent\ Prediction\ Residual\ Detection
}
$$

这三个技术模块已经足够形成一篇完整 paper。

---

# 61. 项目最终结构总结

```text
WORLDNTN
│
├── 1. Physics Backbone
│   ├── Orbit
│   ├── Geometry
│   ├── Visibility
│   ├── Doppler
│   └── FSPL
│
├── 2. Learned World Model
│   ├── Graph Encoder
│   ├── Latent Dynamics
│   ├── Action Conditioning
│   ├── Multi-step Prediction
│   └── Uncertainty
│
├── 3. Predictive Orchestration
│   ├── Satellite Association
│   ├── Handover
│   ├── Load Balancing
│   └── MPC / Counterfactual Rollout
│
├── 4. Resilience
│   ├── Prediction Residual
│   ├── Anomaly Detection
│   ├── Jamming / Failure
│   └── Uncertainty-Aware Detection
│
└── 5. Autonomous Recovery
    ├── Candidate Mitigations
    ├── Imagined Rollouts
    ├── Robust Utility
    └── Best Action Execution
```

最核心的一句话仍然是：

$$
\boxed{
\text{Known physics tells us what should happen geometrically;}
}
$$

$$
\boxed{
\text{the world model learns what happens operationally;}
}
$$

$$
\boxed{
\text{the mismatch tells us when the world is abnormal.}
}
$$

---

# 62. 近期相关文献与定位

以下文献是设计这个方向时最需要关注的几个“相邻工作”。

1. **Hang Zou et al., “Telecom World Models: Unifying Digital Twins, Foundation Models, and Predictive Planning for 6G,” 2026.**  
   提出了 action-conditioned、uncertainty-aware telecom world model 和 predictive planning 的总体概念。  
   https://arxiv.org/abs/2604.06882

2. **Ziqi Chen et al., “A Wireless World Model for AI-Native 6G Networks,” 2026.**  
   使用 joint-embedding predictive architecture 与多模态 Transformer 融合 CSI、3D geometry/point cloud 和 trajectory，主要聚焦 wireless propagation world model。  
   https://arxiv.org/abs/2603.25216

3. **J. Zheng et al., “From Digital Twins to World Models,” 2026.**  
   系统讨论 Digital Twin 向 World Model 的演进以及 predictive / agent-centric modeling。  
   https://arxiv.org/abs/2603.17420

4. **“Physics-Informed Digital Twins for Channel Estimation and Traffic Prediction of Non-Terrestrial Networks,” 2026.**  
   已经将 orbital dynamics、channel / weather 和 traffic 建模结合到 NTN digital twin，因此本项目不能只停留在 “physics-informed DT”。  
   https://arxiv.org/abs/2605.23155

5. **Xingqiu He et al., “PreHO: Predictive Handover for LEO Satellite Networks,” 2026.**  
   展示了利用 LEO orbit/ephemeris 进行 predictive handover 的价值，因此 WorldNTN 必须进一步处理随机 traffic、load、interference 和 action-conditioned network dynamics。  
   https://arxiv.org/abs/2603.07987

6. **Aruna Jayarajan et al., “LEO Satellite Network Orchestration with Heterogeneous Graph Neural Networks,” 2026.**  
   使用 heterogeneous GNN 对动态 satellite-ground graph 做实时 orchestration，说明 graph 是自然表示方式，但并非 WorldNTN 的核心 novelty。  
   https://arxiv.org/abs/2606.31950

7. **“JEPA for AI-Native 6G: Predictive Representations and Open Challenges,” 2026.**  
   讨论 predictive representation、normal-state modeling 和 anomaly detection，包括 jamming/spoofing 等安全应用，为 Graph-JEPA 版本提供直接参考。  
   https://arxiv.org/abs/2607.09798

8. **ETSI TR 104 319 work item, “A reference functional architecture for integrating Network Digital Twins in 6G systems,” 2026.**  
   ETSI 已在推进面向 6G 的 Network Digital Twin 功能架构，包括 AI-driven orchestration、simulation 和 data management。  
   https://portal.etsi.org/webapp/WorkProgram/Report_WorkItem.asp?WKI_ID=79357

9. **3GPP Release 20.**  
   Release 20 同时涉及 AI/ML、Satellite/NTN、Energy Efficiency/Sustainability，并启动 early 6G studies。  
   https://www.3gpp.org/specifications-technologies/releases/release-20

---

# 63. 最后的技术选择建议

如果现在立刻开始做，我会按这个顺序：

1. **先不要加 LLM。**
2. **先不要直接做大 Transformer。**
3. 搭好 physics NTN simulator。
4. 建 stochastic traffic/channel/interference，使 network dynamics 真正有“未知量”。
5. 先做 **GRU predictor baseline**。
6. 做 **physics-conditioned latent state-space model**。
7. 加 action conditioning。
8. 上 **MPC**。
9. 用同一个模型做 **normal-state anomaly detection**。
10. 加 jammer。
11. 做 mitigation rollout。
12. 最后再决定是否升级为 heterogeneous graph / JEPA。

最值得保住的 research identity 是：

> **WorldNTN is not an AI replacement for orbital physics. It is a learned model of the uncertain network dynamics that remain after known NTN physics has been factored out.**

这句话基本定义了整个项目。
